# NetworkToys 開発メモ

Windows ネイティブのネットワーク診断ツール。C# + WPF + .NET 10 (LTS)。

## この作業環境で最初に理解すべきこと

**開発コンテナは Linux で、dotnet SDK が無く、Windows 側ドライブも見えない。exe を一度も実行できない。**
コンパイルすらローカルではできないので、**GitHub Actions が唯一のコンパイラであり、唯一の実行環境**。

そのための設計:

1. **`NetworkToys.Core` (net10.0) に純粋ロジックを押し出す** — ネットワークにも WPF にも触らないコード（IP 範囲パース、統計、MOS 算出、OUI 解決、レポート生成）はすべてここへ。CI の xUnit で検証できる面積を最大化する。
   原則: **テストできない部分（WPF・P/Invoke・実ネットワーク）を、テストできる純関数の薄いラッパーに追い込む。**
   例: traceroute なら「Ping を投げる部分」と「ホップ列を解析・整形する部分」を分け、後者を固定データでテストする。
2. **`--selftest`** — `NetworkToys.exe --selftest` で自己診断し、終了コードを返す。**XAML のエラー（リソースキーの打ち間違いなど）はコンパイルを通り抜けて実行時に落ちる**ので、MainWindow を実際に生成・表示するこの検査が実質唯一の防波堤。新しい画面やリソースを足したら必ずここに検査を追加する。
   **ウィンドウは `Show()` してから `UpdateLayout()` すること。** Show せずに `Measure()` を呼んでもテキスト整形まで到達せず、フォント初期化の不具合を見逃す（InvariantGlobalization の事故で実証済み）。
   **終了コードが 0 でも `crash.log` があれば CI は失敗する。** 投げっぱなしのタスクから上がる例外はプロセスを落とさないので、終了コードには一切現れない。表示するだけにしていた頃、FTP サーバの `ObjectDisposedException` が毎回出ていたのに緑のまま通り続けた（→「投げっぱなしのタスク」）。**この判定を外さないこと。**
3. **1 フェーズ = 1 まとまり = CI グリーン** — 大きな差分を積むと失敗の切り分けが不可能になる。

## 実装上の罠（調査済み。踏む前に読むこと）

### ビルド・配布

- **WPF はトリミングできない。** `PublishTrimmed=true` は NETSDK1168 でビルドエラー。`PublishAot` も不可。self-contained のサイズは削れない前提で、「軽量」は起動時間とメモリで満たす。
- **`PublishSingleFile` + `PublishReadyToRun` は起動失敗の報告がある**（dotnet/wpf#7282, #11436）。Phase 0 のマトリクスで実測して採用形を決める。
- **単一ファイルでは `Assembly.Location` が空文字を返す。** exe 横のファイルは `AppContext.BaseDirectory`、自分の exe パスは `Environment.ProcessPath` を使う。
- **`InvariantGlobalization` は WPF では使えない（CI で実測）。** `true` にすると ICU が同梱されず、`MS.Internal.FontCache.MajorLanguages` の型初期化に失敗して**最初の TextBlock を測った瞬間に落ちる**。厄介なのは `dotnet publish` が正常に通ることで、ウィンドウを表示して初めて死ぬ。サイズ削減のために再度有効化しないこと。
- 圧縮（`EnableCompressionInSingleFile`）はサイズを大きく削るが、圧縮アセンブリはメモリマップできず起動時展開が要るので R2R と打ち消し合う。

### 測定

- **`Ping` インスタンスは同時に 1 リクエストしか扱えない。** 進行中に再度 `SendPingAsync` すると `InvalidOperationException`。宛先ごとに 1 インスタンスを持たせ、その宛先内では逐次にする。traceroute の TTL 並列など 1 宛先で同時に複数投げる場面ではインスタンスをプールする。
- **`PingReply.RoundtripTime` は `Status != Success` のとき信用できない**（0 が返る）。RTT は必ず自前の `Stopwatch.GetTimestamp()` で測る。
- **`Task.Run` で同期版を包まない。** `Ping`/`Socket` の非同期 API は I/O 完了ポートを使うので数百同時でもスレッドを消費しないが、同期版を包むとスレッドプールが枯渇する。
- **traceroute の TTL 完全並列は欠落ホップを生む。** ICMP TTL 超過はルータ側でレート制限されるため、TTL ごとに 20〜30ms のスタガーを入れる。確実性優先の逐次モードも設定で用意する。
- **TCP ping はエフェメラルポートを食う。** FIN で行儀よく閉じると TIME_WAIT が約 4 分残り、200 宛先 × 1 秒間隔なら数分で枯渇する。採用した対策は **`LingerState = new LingerOption(true, 0)` の RST クローズ**（TIME_WAIT を残さない）。測定用の接続はデータを流さないので RST で切っても相手は困らない。ソケットを触る箇所を増やすときは必ずこれを踏襲する。
- **Path MTU にはブラックホールがある。** ICMP「要フラグメント」を返さないルータがあると、大きすぎる場合が `PacketTooBig` ではなく `TimedOut` になる。両者を区別して記録し、UI でも書き分ける。
- **経路変化は ECMP で誤検知しやすい。** 2 回連続で異なった場合のみ「変化」と判定する。

### Windows API

- **Wi-Fi 情報は Windows 11 24H2 以降、位置情報の同意がないと `ERROR_ACCESS_DENIED` になる。** 対象は `WlanScan` / `WlanGetAvailableNetworkList` / `WlanGetNetworkBssList` / `WlanQueryInterface`(現在の接続)。ManagedNativeWifi は `UnauthorizedAccessException` として投げる。
  - 起動時にスキャンしない。Wi-Fi 画面を開く、またはユーザーが「スキャン」を押したときに初めて呼ぶ。
  - 例外を捕捉して `ms-settings:privacy-location` への導線を出す。
- **接続環境の判定を SSID に依存させない。** 上記の理由で SSID は取れないことがある。有線でも成り立つよう、ローカル IPv4 サブネットとデフォルトゲートウェイを先に見る。
- **`WlanScan` の再スキャン間隔は最短 5 秒、既定 10〜15 秒。** Windows の WLAN サービス自体が既定 60 秒間隔でしかスキャンしないので、短くしても新しい結果は返らない。
- **プロキシ設定は Windows に 2 つある。** ①**WinINET**（`HKCU\…\Internet Settings`）はログインしたユーザーごとで、ブラウザが従う。管理者権限は不要。②**WinHTTP** は PC 全体で 1 つで、サービスとして動くもの（Windows Update・監視エージェント・.NET のアプリ）が従う。**片方だけ直すと「ブラウザは見えるのに更新だけ出られない」になる**ので、IP設定タブでは両方を並べている。
  - **WinHTTP の読みは API から。** `netsh winhttp show proxy` の出力は表示言語で変わるのでパースしない。`WinHttpGetDefaultProxyConfiguration` を P/Invoke する（読むだけなら管理者権限は不要。返る文字列は `GlobalFree` で返す）。
  - **書きは昇格した `netsh -f`。** `WinHttpSetDefaultProxyConfiguration` は管理者権限が要り、プロセス内で昇格はできない。IP設定の適用と同じ `ElevatedNetsh` に `winhttp set proxy …` の 1 行を流す。
  - **WinHTTP は PAC を持てない。**「直接」か「固定」の 2 つだけ。**選択肢に PAC を出さない**
    （選ばせてから「使えません」と言うより分かりやすい）。上段（WinINET）とは**独立した入力欄**を持つ
    — 片方の値をもう片方に流用する作りにしない（2026-08-16 ユーザー指示で「取り込む」もやめた）。
  - **スクリプトに入れる文字列から落とすのは引用符と改行だけ。** netsh はスクリプトを自分で読むのでコマンドプロンプトを経由せず、`<` `>` `&` に特別な意味は無い。**除外リストの `<local>` は最もよく使う値**なので、落とすと機能が壊れる（実際に一度やった）。
- **`arp -a` をパースしない。** 出力が OS の表示言語でローカライズされるため日本語環境と英語環境で壊れる。`iphlpapi.dll` の `GetIpNetTable2` を P/Invoke する（管理者権限不要）。同じ理由で **`netstat` もパースしない**。接続一覧は `GetExtendedTcpTable` / `GetExtendedUdpTable`（非管理者で全プロセスの PID まで取れる）。この 4 種の行テーブルは**配列が先頭ヘッダの +4 から始まる**。`GetIpNetTable2` の +8 を写経すると 1 行目から壊れる。
- 全機能を**非管理者で動作させる**方針。`app.manifest` は `asInvoker` 固定。例外は 2 つ。①**接続タブの通信量（B/秒）** — ETW のカーネルネットワークイベントが管理者限定のため、非管理者では列を「—」にして案内を出す縮退動作（接続一覧そのものは非管理者で動く）。案内行の「管理者として再起動」ボタンから望む人だけ `runas` で昇格できる（確認ダイアログ付き。UAC 拒否 1223 は握って続行）。②**IP設定タブの「適用」** — アプリ自体は昇格せず、適用の瞬間だけ `netsh -f 一時スクリプト` を `runas` で起動する（複数コマンドを 1 ファイルにまとめるので UAC は 1 回）。
- **適用の直後に現在値を入力欄へ書き戻さない。** `RefreshAdapters` がアダプタを選び直すと現在値が
  入力欄へ入るが、OS の読みが落ち着く前だと固定 IP を当てた直後でも DHCP と報告され、
  **「DHCP で自動取得する」に勝手にチェックが入る**（2026-08-20 ユーザー報告）。
  適用後は plan（設定した本人の意図）を入力欄へ戻す。
- **IP設定（ElevatedNetsh）の罠。** ①昇格プロセスの出力は直接は読めない（リダイレクトと ShellExecute は両立しない）ので、`cmd /S /C "netsh -f … > 出力.txt 2>&1"` でファイルへ落とす（/S は最初と最後の引用符だけ剥がすので空白入りパスが壊れない）。**読みはコードページを決め打ちしない** — netsh はリダイレクト先へ UTF-16 で書くことがあり、cp932 決め打ちで実際に化けた。BOM か NUL バイトで UTF-16 を見分け、残りは UTF-8 厳密 → cp932（`DecodeConsoleOutput`。自己診断が 4 形を固定）。失敗時はその文面を**そのまま画面に出すが、解釈はしない**（ロケール依存。「コード 1」だけでは原因が分からないと 2026-08-21 に報告された）。成否の判定はこれまでどおり ExitCode と、適用後の `NetworkInterface` 再読で行う。UAC の確認が「Windows コマンド プロセッサ」名義になるのは織り込み済み。②スクリプトファイルは **ANSI（cp932）**で書く（UTF-8 だと日本語アダプタ名が `name=` で一致せず静かに失敗する）。③タイムアウトしても昇格した子は Kill できない（AccessDenied）— 放置して現在値の確認を促す。④**selftest から ElevatedNetsh を呼ばない** — CI ランナーは管理者なので UAC なしで本当に適用され、ランナーのネットワークが壊れる。⑤接続情報のフッターは 2026-08-16 にユーザー指示で廃止した（本体を広く取るため）。現在値は IP設定タブで見せる。再提案しないこと。⑥**IP 設定の適用に netsh を使わない。PowerShell（NetTCPIP）の昇格スクリプトで行う**（`ElevatedNetsh.ApplyPowerShellAsync` + `IpPlan.ToPowerShellScript`。BOM 付き UTF-8・番号 `-InterfaceIndex` 指定、取れないアダプタだけ `-InterfaceAlias '名前'`）。netsh は 2026-08-21 に実機で 3 連敗した: (a)日本語アダプタ名が文字コードの食い違いで ERROR_INVALID_NAME（「ファイル名、ディレクトリ名、またはボリューム ラベルの構文が間違っています」）→ 番号化で回避できたが、(b)netsh には **DHCP の旗を直接切り替える命令が無く**、旗と実アドレスが食い違った機械では `set address source=dhcp` が「すでに有効です」の断りだけで何もしない。(c)`delete address` でも `static` の入れ直しでも旗は下りなかった。`Set-NetIPInterface -Dhcp Enabled|Disabled` は旗そのものを切り替えるので、どんな状態からでも同じ結果に揃う。**…と考えたが NetTCPIP も脱落した** — 旗を ActiveStore/PersistentStore の両方へ明示しても `New-NetIPAddress` は「Inconsistent parameters PolicyStore PersistentStore and Dhcp Enabled」で断り続けた（実機で 2 度確認）。最終形は **WMI（`Win32_NetworkAdapterConfiguration`）の `EnableStatic` / `EnableDHCP` / `SetGateways` / `SetDNSServerSearchOrder`** — DHCP↔固定を 1 メソッドで原子的に切り替えるのでストア間の矛盾という概念が無く、**戻り値が数値**（0=成功/1=要再起動）なのでロケール非依存で成否を取れる。古い既定ルートだけは `Remove-NetRoute` で名指しで掃除する。**戻り値 84（IP not enabled）だけは再試行する** — `EnableStatic` 直後は IP スタックが再初期化中で、続く `SetDNSServerSearchOrder` などが一瞬 84 を返す（実機で発生）。生成スクリプトの `Invoke-Nic` がインスタンスを取り直しながら最大 10 秒粘る。84 以外は即失敗。NetTCPIP・netsh に戻さないこと。⑨**リンクダウン中のアダプタは WMI では設定できない**（84 が恒久的に返る。ユーザーの本来の使い方は「機器につなぐ前に仕込む」— 2026-08-21 に判明）。リンク状態で書き分ける: **Up = WMI／Down = NetTCPIP を `-PolicyStore PersistentStore` 明示で**（リンクアップした瞬間に Windows が適用する。DHCP の旗を同じストアへ先に書くので New-NetIPAddress の矛盾エラーも起きない。**リンクダウンの固定化は NetTCPIP では作れない** — `Set-NetIPInterface -Dhcp Disabled -PolicyStore PersistentStore` はエラーなく通るのに永続側の旗が実際には下りず、直後の `New-NetIPAddress` が「Inconsistent parameters PolicyStore PersistentStore and Dhcp Enabled」で落ち続ける。永続単独のアドレス作成も「Invalid parameter」。**固定化だけは netsh の `set address … static` を PowerShell からコマンドライン引数で呼ぶ**（旗とアドレスを一括で書ける唯一の口。かつての文字化けは `netsh -f` のファイル文字コードが原因で、引数渡しなら起きない。`$LASTEXITCODE` で成否を見て netsh の出力ごと throw する））。リンクダウン時は適用後の確認を行わず「リンクアップしたときに有効になります」と伝える（現在値には現れないため）。**IP 設定を netsh に戻さないこと**（`netsh -f` 自体は WinHTTP プロキシで現役。あちらは ASCII のみで cp932 の罠も無い）。⑦**PowerShell の失敗はそのまま失敗として見せる**（応答の文面を添える。解釈はしない）。netsh 時代の「現在値が合っていれば成功扱い」は、DHCP のリースが目標の固定アドレスと同値のときに本当の失敗を「✓ すでにこの内容」で握り潰した — NetTCPIP に「すでに有効です」のような無害なエラーは無いので、包む理由が無い。成功時だけ、反映待ちを粘って現在値（モードとアドレスの両方）と突き合わせる。⑧**「固定か DHCP か」は旗（IsDhcpEnabled）でなくアドレスの出自（PrefixOrigin）で判定する** — 旗は「有効のまま手動アドレスが付いている」混成状態で嘘をつく（固定にしたのに DHCP と表示された）。
- **ETW（通信量表示）の罠。** ①セッション名は固定（`NetworkToys-KernelNet`）なので、**開始前に必ず `ControlTrace` STOP で前回の残骸を掃除**する（クラッシュ後はセッションが OS に残り続ける）。②ネイティブに渡すコールバックの**デリゲートはフィールドに保持**しないと GC に回収されて即死する。③`ProcessTrace` はセッションが止まるまで返らないので専用の背景スレッドで回し、停止は `CloseTrace` → `ControlTrace` STOP → `Join(1秒)`。④TraceEvent（NuGet）は不採用 — 必要なのは 1 プロバイダ・8 イベント ID の固定レイアウトだけで、ローカルで検証できないバージョン依存の方がリスクが大きい。⑤`EVENT_RECORD` のレイアウト誤りは catch 不能の AccessViolation でプロセスごと落ちる。防波堤は自己診断の ETW 検査（CI の Windows ランナーは管理者なので実経路が通る）。非管理者の縮退表示だけは CI で踏めないため実機確認。

### 一覧の列幅と並べ替え

- 列幅は `ViewModels/TableColumns.cs` に表ごとの宣言を置き、見出しと行の両方が同じキー（`テーブル名.列番号`）で引く。**列を足し引きしたら宣言も直す**（番号は XAML の `Grid.Column` と 1 対 1）。Ping/TCP 一覧だけは先にできた `ColumnLayout` を使い続けている。
- **列幅のドラッグは「掴んでからの総移動量」で決める。** 1 回ぶんの差分
  （`DragDelta.HorizontalChange`）を足し込んではいけない — 幅を変えるとつまみ自体が動くので、
  `Thumb` は自分の移動ぶんを差し引いた値を返す。足し込むと伸び縮みが鈍り、端で詰まると飛ぶ
  （2026-08-17 に実機で「変な動き」として報告された）。掴んだ時点の幅とマウス位置を
  `DragStarted` で控え、`TableColumns.Resize` に総移動量を渡す。
- **つまみが表すのは「列」ではなく「列と列の境目」。掴んだ境目の左右の列だけが伸縮する**
  （2026-08-18 ユーザー指示。以前は掴んだ列だけを動かし、余りを星列に吸わせていたので、
  離れた列が勝手に伸び縮みして見えた）。星列より左の列は右端に、右の列は左端に
  つまみを置いてあるので、**つまみの列番号から境目の左右が決まる**
  （`TableColumns.BoundaryOf` / `ColumnLayout.Boundaries` の 1 か所）。
  片側が星列のときは、もう片側だけを動かせばよい（星列が同じだけ吸う）。
  **どちらかが下限・上限で詰まったら移動量をそこで止める** — 止めないと境目が
  カーソルから離れて、片側だけ伸び続ける。
- **つまみは見出しの Grid に置く。行テンプレートに入れると全行に付く**（実際に一度やった）。見出しが無い一覧には見出しを作ってから置く。この「つまみを持つ Grid＝見出し」という性質を、自己診断が見出しを見つける目印にも使っている。
- **画面を左右/上下に割る比率も保存しない**（列幅と同じ決まり）。仕切りは固定幅のトラック
  （6〜11px）に **`PaneGrip.V/.H` スタイルの `Thumb`** で置き、**星のトラックには必ず
  MinWidth/MinHeight を入れる**（xUnit が突き合わせ、総数も固定）。
  **GridSplitter を使わない** — 星サイズ＋Min の組で端までドラッグすると比率が跳ね戻る
  既知の癖がある（2026-08-20 実機報告）。列幅ドラッグと同じ「総移動量＋`PaneClamp` の
  明示クランプ」で Min にぴったり止める（クランプの端の挙動は自己診断が固定）。表の列検査は `TableColumns`
  の列を持つ Grid しか見ないので、分割用の Grid に星が増えても引っかからない。
  **差分比較の結果は見出しと一覧を 1 つの Grid に併合してある** — 別々に戻すと、仕切りを
  動かしても見出しが付いてこない。**開閉式の区画に仕切りを入れるときは、閉じたら行ごと畳む**
  （Ping の宛先リスト = `PingEditorRow` / `OnTargetListToggle`。ドラッグで星になった行は
  Visibility を畳んでも空のまま場所を取り続けるので、Auto + Min 0 に戻し、開いたら前回の
  高さへ戻す。**開いたときの既定は結果と同じ 1\***（Auto だと中身なりに伸びて、宛先が多いとき
  仕切りが画面の外へ行く）。自己診断が開閉の往復を固定）。
- **列幅は保存しない**（2026-08-18 ユーザー指示。毎回そろった幅で始める）。
  ドラッグした幅はそのプロセスの中だけで、`settings.json` には入れ物ごと置いていない。
  おかげで**既定を直せばそのまま画面に出る**（保存していた頃は、古い幅が残っている人には
  直した幅が出なかった）。**宣言の値は標準の文字の大きさのもの**なので、
  組むときに `UiScale.Current` を掛ける。
- **削除の列に ✕ 以外のボタンを足したら幅も足す。** 業務確認は ✕ と ▶ の 2 つが並ぶので
  37px 要る。ほかの表の削除列（28px）をそのまま写して ▶ が切れていた（2026-08-18 報告）。
- **固定幅の列ばかりの表は、可変幅(星)列に `MinWidth` を入れる。** 既定の窓幅（880px）で
  使えるのは 800px 前後しかなく、固定幅が 7 列もあると星列が数文字ぶんまで潰れる
  （ログ採取の「機器のアドレス」で IP が読めなくなっていた。2026-08-18 報告）。
  **見出しと行の両方に同じ値**を入れること（片方だけだと列が噛み合わない）。
- **右揃えの列には右の余白を 10px 入れる**（2026-08-18 ユーザー指示）。右に寄せた数字と、その右隣の左揃えの文字がくっつくと、どこで列が切れているのか読めない。**新しく右揃えのセルを足すときも同じ**（`HorizontalAlignment="Right"` と `Margin="0,0,10,0"` は対で書く）。見出しと行の両方に入れること — 片方だけだと数字の位置がずれて見える。
- **見出しと行のズレは xUnit が捕まえる**（`tests/…/MainWindowTableTests.cs`。2026-08-19 に
  規約そのものを検査にした — 同じ指摘が何度も出ていたため）。`MainWindow.xaml` を
  テストへ**埋め込んで文字として読み**、①同じ表の列定義（幅の出どころ・星・`MinWidth`）が
  見出しと行で 1 字も違わないこと ②列を持つ一覧が行コンテナと縦スクロールバーの場所を
  確保していること ③Ping の見出しの余白が `11,0,25,3` であることを見る。
  **列を足し引きしたら、見出しと行の両方を直す**（片方だけだと必ずここで落ちる）。
  実際にこれで見つかった: ACI のポートの行に `aciport.10` が 2 回書いてあり、
  列が 1 つ多いぶん**可変幅より右が全部ずれていた**。
- **Ping / TCP の一覧も縦スクロールバーの場所を常に取る**（`TargetListBox` スタイル）。
  見出し側は右の余白でそのぶんを先に空けてある（左 11 / 右 11 + 14 = 25）。
  取らないと、行が増えてバーが出た瞬間に**備考より右の 3 列だけ 14px ずれる**。
- **見出しと行の左右の余白は 1 つの規約に揃える。** 見出しは一覧の外にある別の Grid なので、食い違うと列が噛み合わない。**見出しの `Margin` は `4,0,18,3`、行は `PlainRowContainer`（`Padding="4,0"`）、一覧は縦スクロールバーを常時確保**（`ScrollViewer.VerticalScrollBarVisibility="Visible"`）。右の 18 は行の 4 + スクロールバーの 14 で、幅は `Controls.xaml` の `ScrollBar` スタイルで 14px に固定してあるから予約が確実に合う。自己診断が全タブを回って突き合わせる。
  - 実際に踏んだずれ方 3 つ: ①行テンプレートを包む枠にも `Padding="4,0"` を書いて**二重に 4px** 入った（接続のプロセス見出し行）②`ItemContainerStyle` の指定漏れで**既定の `ListBoxItem`**（枠 1px + `Padding="2,0,0,0"`）で描かれた（Meraki の 4 一覧）③スクロールバーが出た瞬間に**行だけ 14px 狭くなり**、可変幅(星)列より右が全部ずれた。**③は 16 表のうち 10 表が該当した** — 星列が最後にある表だけは星が縮んで吸収するので見えない。
  - **行を包む枠に余白を足さない。** 左右は行コンテナが持っている。地色を敷きたいだけなら `Background` だけ指定する。
- **並べ替えできる見出し（`OnTableHeaderSort` の対象）には `Cursor="Hand"` と ToolTip を付ける**
  （2026-08-20 の UI 改善。押せることが見た目で分からなかった）。自己診断が `SortPaths` と
  突き合わせて欠落を捕まえる。新しい表を足すときは見出し Grid に 2 属性を忘れない。
- 並べ替えは 2 系統ある。自前で並べ替える一覧（Ping/TCP・接続・無線・遮断）は VM の `SortBy` を `OnListHeaderSort` から呼ぶ。それ以外は `OnTableHeaderSort` が `ICollectionView.SortDescriptions` に任せる（VM を触らずに済み、仮想化も効いたまま）。**列は見出しのクリック位置から求める** — 列ごとに当たり判定を置くとヘッダ 1 つにつき数個ずつ増えるため。
- **一覧は `FindList` が見出しから探す。見出しと同じ `Tag` を持つ `ItemsControl` を先に見る。** かつては `ListBox` しか見ておらず、`ItemsControl` で組んだ経路のホップ一覧だけ並べ替えが無反応だった。かといって「最初の `ItemsControl`」にはできない — 経路タブには手前に「何が変わったか」の一覧があり、**`ComboBox` も `ItemsControl`** なので別物を掴む。

### 一覧から次の道具へ送る（右クリック）

- メニューは `MainWindow.xaml` の `Window.Resources` に **`RowMenu.Device` と `RowMenu.Address` の 2 本だけ**置いて、6 つの一覧（スキャン・接続・遮断・Meraki・syslog・Trap）で使い回す。`Controls.xaml` には置けない（別辞書には `x:Class` が無く `Click` を結線できない）。
- **`x:Shared="False"` を外さないこと。** 1 つの実体を複数の一覧で共有すると `PlacementTarget` が取り合いになる。
- **アドレスの取り出しは `MainWindow.AddressOf` 1 か所。** 行の型は一覧ごとに違うので、**新しい一覧にメニューを付けたらここに 1 行足す**。足し忘れると「右クリックしても何も起きない」と黙って壊れるので、自己診断が行の型ごとに実際に取り出して確かめている。
- ホストとポートの分解は**コロンが 1 つのときだけ**切る（素の IPv6 を壊さない。`TargetListParser` と同じ規則）。角かっこ付きは中身を使う。

### 収集タブ（Cisco へ入って集める）

- **状態機械は `Stream` の上に書いて Core に置く**（`Core/Terminal/CiscoSession.cs`）。App に残すのは「`TcpClient` を作る」「`SshClient` から `ShellStream` を取る」だけ。こうすると**メモリ上の偽 Cisco 機器と会話させて xUnit で完走できる**（`tests/…/CiscoSessionTests.cs`）。CI が唯一の実行環境というこのリポジトリでは、ここが正しさの唯一の拠り所。実際にこの検査が「`--More--` を検出前に消していた」「すでに特権の機器へ enable を二重に投げていた」「無音タイムアウトが使われていなかった」の 3 つを捕まえている。
- **ソケットの読み取りをキャンセルしない。** OS がすでに受け取ったバイトを取りこぼすことがある。読み取りは専用タスクに回しっぱなしにして、本体は「届いた合図」を待つ。止めるときは**接続を閉じて**タスクごと終わらせる。
- **プロンプトは推測せず学習する。**「①バッファ末尾にある ②学習したホスト名と一致 ③`>` `#` `)#` で終わる」の 3 条件を同時に満たしたときだけ。①が効くのは、機器の出力行は必ず改行で終わるのにプロンプトだけは改行せず止まるため。**コマンドのエコーは待ち条件にしない**（返さない機器でハングする）。
- **ページャ無効化は機種で出し分けないが、通ったところで止める。** `terminal length 0` → `terminal pager 0` → `terminal width 0` の順に投げ、**機器が断らなかった 1 本目でやめる**（断り文句は `WasRejected` が見る）。以前は 3 本とも投げていたが、IOS-XE で `terminal pager 0` の「% Invalid input」が出て**失敗したように見える**と指摘された（2026-08-18）。機種判定を書かないという考えは変えない — 順に試すだけ。
- **`--More--` は検出してから落とす。** 整形の段で先に消すと `EndsWithMore` が永久に false になり、ページャで止まる（実際に踏んだ）。
- **パスワードを 2 回聞かれたら認証失敗として即中断。再試行しない**（TACACS+/AD でアカウントを固めない）。回数による判定が機種ごとの文言に依存しない一番強い規則。
- **`[confirm]` を検出したら何も送らずに離脱する。** 答えなければ実行されない。ここで Enter を送る実装にしないこと。
- **機器は並行して取りに行く**（同時 8 台まで。2026-08-18 ユーザー指示）。会話は機器ごとに独立しているので順番に待つ理由が無い。**上限は残す** — 踏み台や TACACS+/AD へ一度に殺到させない。待ちの間は UI スレッドへ戻す作りにしてあるので（`ConfigureAwait(false)` を付けない）、行の状態と件数に鍵は要らない。
- **SSH では Enter を 1 文字だけ送る（`\n`）。`CR LF` を送らない。**
  SSH には Telnet のような改行の取り決めが無く、機器側の端末制御がそのまま働くので、
  <b>CR LF は Enter 2 回</b>になる機器がある。1 本のコマンドにプロンプトが 2 つ返り、
  1 つ目で「そろった」と見て次を送るため、遅れて届く 2 つ目が<b>次のコマンドの完了</b>と
  読み違えられて全部ずれる（2026-08-18 の実機報告: show clock は正しく、show version が
  0.0 秒 / 0 行、show inventory の区画に show version が載る）。**Telnet は CR LF が
  Enter そのものなので変えない**（`CiscoSession._newline` を `usedSsh` で切り替える）。
- **「エコーも出力も無いプロンプト」は前のやり取りの残り。** 一度でもエコーを見た機器なら、
  それを捨てて待ち続ける（`CommandCapture.IsBarePrompt`）。エコーを返さない機器では
  この判断を始めない（無出力のコマンドが永久に終わらなくなる）。
- **収集したファイルの末尾に生ログ（届いた文字そのもの）を残す。**
  区画のずれや取りこぼしは実機でしか出ず、これが無いと切り分けられない
  （WLC の「取得した出力」と同じ考え方）。**送った文字は残さない** — パスワードを書かないため
  （機器はパスワードをエコーしない）。自己診断は<b>秘密そのもの</b>を名指しで探す
  （`Password:` のような機器側の文言は当然出るので、`"pass"` のような部分一致で見ない）。
- **呼び水（黙った機器へ入れる改行）への応答を、次のコマンドへ持ち越さない。**
  機器は我に返ったときに**本来のプロンプトと呼び水への応答の 2 つ**を返す。1 つ目で
  「そろった」と見て次を送ると、遅れて届く 2 つ目を**そのコマンドの完了**と読み違え、
  そのコマンドは空で終わり、本当の出力は次の区画に入る。**これが最後まで連鎖する**
  （2026-08-18 に「コマンドがズレまくる」として 2 度報告された。プロンプト待ちだけでは直らない）。
  手当ては 2 つ: ①**呼び水は機器が黙り込んでいるときだけ入れる**（すでに話している機器を突かない。
  無条件の 1 本目は廃止した）②**呼び水を入れた後は機器が言い終わるまで読み捨ててから進む**
  （`DrainAsync`。静穏の物差しは `SettleTime` ではなく `DrainQuiet`＝既定 1 秒 —
  遅れて届くのは**およそ 1 往復ぶん後**なので、短いと取りこぼす）。
  呼び水を 1 度も入れていないセッションでは読み捨て自体を飛ばすので、待ち時間は増えない。
- **コマンドを送る前に、機器がプロンプトで待っていることを確かめる**（2026-08-18 ユーザー指示）。
  前のコマンドが打ち切り・確認待ち・ページャで終わっていると機器はまだ出力の途中で、
  そこへ打ち込むと文字が出力に紛れ、**以降のコマンドと出力の対応が丸ごとずれる**。
  `CiscoSession._atPrompt` はプロンプトを見たときだけ立ち、何か送った時点で降りる。
  降りていたら `ResyncAsync` → `WaitForPromptAsync`（**時間ではなくプロンプトを待つ**）で立て直し、
  それでも返らなければ**そのコマンドは送らずに「実行しませんでした」と記録する** —
  ずれたまま最後まで走るより、1 本落ちる方がよい。
  正常に終わった直後は既にプロンプトを見ているので、待ち時間は 0。
- **プロンプトが返っていれば立て直さない。** 機器が「そんなコマンドは無い」と言って戻ってきた場合、会話はそろっているので `ResyncAsync` は不要。ここで毎回 3 秒待っていたため、機種違いの一覧を流すと待ち時間が積み上がっていた（2026-08-18 報告）。
- **打ち切りの上限は「保険」であって平常の道具ではない。** 平常の終端は無音
  （`IdleTimeout`）とプロンプト検出で決まる。総時間（`CommandTimeout`＝30 分）と総量
  （`MaxOutputChars`＝64 MB）は、黙らないまま終わらない出力を最後に切るためだけの値 —
  180 秒 / 4 MB にしていた頃、遅い回線の show tech-support が途中で切れた（2026-08-21 報告）。
  **流れ続けている間は正当な出力**なので、この 2 つをきつくしないこと。
- **危険なコマンドは実行前に弾く。** `reload` などは対話プロンプトを返し、状態機械は答えられない（答えてはいけない）ので、投げても「なぜか動かない」にしかならない。**上書きのチェックボックスは作らない**（作った瞬間に弾く意味が消える）。
- **「失敗だけ再実行」の失敗は ✕ と ⛔ だけ**（`CollectViewModel.IsFailedStatus`。2026-08-20 の機能改善）。
  **△ 一部失敗は含めない** — 出力は取れており、上書き再収集はかえって証跡を汚す。
  ⛔（パスワード未入力で飛ばした行）は、入れ直して再実行する対象なので含める。
  収集後に鍵を捨てる決まりはそのまま — 再実行の前に入れ直してもらう。
- **パスワードは保存しない。入れ物も作らない。** `AppSettingsDocument` にも `HandoverPanels` にも `Password` を含むプロパティを置かないこと（自己診断が名前で検査している）。覚えるのはユーザー名だけ。
- **機器の一覧だけ仮想化を切る。** 行コンテナを使い回すと `PasswordBox` はバインドを持たないので `DataContext` が変わっても中身が入れ替わらず、**スクロールしただけで別の機器の欄に前のパスワードが載る**（そのまま送信されうる）。「一覧は仮想化必須」への明示的な例外。
- 収集した出力は `collect\` **直下**に置く（2026-08-20 ユーザー指示で回ごとのフォルダをやめた。
  ファイル名に機器名と日時が入るので混ざらない）。**`logs\` には置かない** — `SessionLogService.CleanupOldLogs` が 30 日で `.log` を消す。
- **機器一覧の CSV は `DeviceListParser.ParseCsv`**（`宛先,ssh|telnet,ユーザー名,パスワード,enable,メモ`。
  2026-08-20 ユーザー指示でパスワードの列を足した）。**パスワードは `ImportedDevice` の側だけが持ち、
  `DeviceEntry` には入れない** — あちらは settings.json へ往復する（型で分けてあり、xUnit が
  `DeviceEntry` にパスワードのプロパティが無いことを固定している）。**この書式を書き出すのは
  ひな型だけ**（実物のパスワードを書き出す口は作らない）。取り込んだパスワードは
  `SecretsImported` → `FillCollectPasswordBoxes` で伏せ字欄へ映す（PasswordBox はバインド不可のため）。
- **`Path.GetInvalidFileNameChars()` を使わない。** Linux では `/` と NUL しか返さず、開発機と CI でテストの結果が変わる。禁止文字は明示列挙する。

### 差分の取り方（差分比較・ACI の差分比較）

- **2 万行を比べられること。**（2026-08-17 ユーザー指示）素の LCS は表が面積ぶん要るので、
  **一意な共通行で区切ってから小さく解く**（patience diff。`Core/Work/TextDiff.cs`）。
  設定ファイルは同じ行がほとんどなので、これで実用的な速さになる。
- **共通の行が 1 本も無い区間は「まるごと削除 → まるごと追加」で見せる。**
  諦めて空の結果を返さない（何も出ないと壊れて見える）。
- **`TooLarge` は 20 万行を超えたときだけ。** 面積では諦めない。
- **一覧へ 1 行ずつ Add しない。** 2 万行を `ObservableCollection.Add` で入れると、
  そのたびに一覧が測り直されて数秒固まる。`BulkObservableCollection.Reset` で
  まとめて入れ替える（Ping の測定結果のような逐次更新には使わないこと）。

### 差分比較の「機器から」

- **会話を書き直さない。** 収集タブと同じ `DeviceCollector`（＝Core の `CiscoSession`）へ
  コマンド 1 本で投げるだけ。ページャ無効化・プロンプトの学習・パスワード 2 回で中断は
  あちらが面倒を見る。**2 本目の実装を作らないこと。**
- **流すコマンドは比較する対象から決める**（`DeviceComparison.CommandFor`）。対象と違うものを
  流すと、比べ方だけが構造化されて中身が噛み合わない。「そのまま比較」は決まった形が無いので
  **空文字**（勝手に何かを流さない）。
- **打ち替えは許すが `CiscoCommandGuard` は通す。** 収集タブと同じ物差しで、
  **上書きの手段は作らない**（作った瞬間に弾く意味が消える）。
- **パスワードは持ち帰らない・保存しない。** 覚えるのはプロセスの中だけ（接続先・ユーザー名・方式）で、
  `settings.json` にも `HandoverPanels` にも入れ物を作らない。
- **読み込みは置き換え。** ファイルから読むときと同じ（混ざると誤った差分を見る）。

### 昇格時の状態引き継ぎ

- 「管理者として再起動」は、いまの状態を `%TEMP%` の引き継ぎファイルへ書き、`--resume <path>` で次のプロセスへ渡す（`Services/HandoverService.cs`）。**受け取った側が真っ先に消す**（`HandoverStore.LoadAndDelete` が finally で必ず削除する）。UAC を断られた場合は書いた側が消す。差分比較の貼り付けには `show run` の enable secret のような認証情報が入りうるので、ここを緩めないこと。
- **WFP と通信状況から昇格したときは「一時停止」を外して開く**（2026-08-18 ユーザー指示）。
  権限が足りなくて見えないから昇格したのであって、また止まった状態で開くと押し直しが要る。
  判定は引き継ぎファイルの `SelectedTab`。
- **控えるのは「押したボタンが載っているタブ」**（`MainWindow.TabOf`。論理ツリーでたどる）。
  外側の `MainTabs.SelectedItem` を控えると、束ねたタブの中（通信状況）から昇格したときに
  **「その他」の先頭の画面で開き直してしまう**。ボタン以外（メニュー）から押されたときは
  いま見えているタブ（`ShownTab`。内側まで下りる）を控える。
- **`settings.json` と役割を混ぜない。** あちらは「次の起動でも残っていてほしい設定」、こちらは「いま画面に出ているもの」で一度きりの受け渡し。新しい引き継ぎ項目は `HandoverDocument` に足す。
- **Meraki の API キーは引き継がない。** 保存しない決まりで伏せ字にしているのに、引き継ぎファイルへ書けば平文でディスクに落ちる。`HandoverPanels` にキーの入れ物を作らないこと（自己診断が入れ物の不在を検査している）。
- **作業窓の集計（`WindowCounters`）は履歴の再生で復元しない。** リングバッファは古い分を捨てるので、窓を開いてから溢れたぶんの試行回数は数え直せず、作業前後の比較が静かにずれる。`WindowCounters.Restore` で値をそのまま書き戻す。履歴の再生で組み直すのは状態機械と RTT 統計だけ。
- 引き継ぎに失敗しても**起動はさせる**（版が違えば捨てる／例外は握って `CrashLog` へ）。引き継げないより起動しない方が困る。
- 待受中のサーバは引き継げない（ソケットはプロセスをまたげない）。確認ダイアログでその旨を断り、自動では開き直さない。

### WFP（遮断タブ）

- **構造体を 1 つも宣言しない。** `FwpmNetEventEnum2` が返すのは `FWPM_NET_EVENT2***`＝**ポインタの配列**で、配列のストライドは構造体サイズではなく常に 8。だから `Marshal.SizeOf` を必要とする箇所が設計上どこにも無く、ETW で踏んだ「構造体サイズを誤って隣を踏む」事故が起こりようがない。必要な 10 フィールドだけを `Marshal.Read*` でオフセット直読みする（`PtrToStructure` は構造体全体を読むので、末尾フィールドの誤りだけでバッファ外へ出る）。**構造体宣言に「戻さない」こと。**
- **`type`（オフセット 104）の値域検査を union の逆参照より前に必ず通す。** ヘッダ長（`FWPM_NET_EVENT_HEADER2` = 104 バイト）の思い違いだけが catch 不能の AccessViolation に直結する唯一の経路で、そこを値域検査 1 つできれいなエラー文字列に変換している。
- **WFP のアドレスとポートはホストバイトオーダー。** `iphlpapi` の行テーブル（接続タブ）はネットワークバイトオーダーなので、あちらを写経すると `192.168.1.10` が `10.1.168.192` になる。変換は `Core/Net/WfpBlockedEvents.cs` の `WfpFormat.Ipv4FromHostOrder` 1 か所に閉じてある。
- **`FwpmFreeMemory0` は `entries` に 1 回だけ。** 配列とイベント本体・DROP2・appId の実体は 1 つの割り当てなので、個々のイベントポインタに対して呼ぶと二重解放になる。`FwpmNetEventDestroyEnumHandle0` はエンジンハンドルを引数に取るので、エンジンを閉じる**前**に呼ぶ。生ポインタをメソッドの外へ出さない（遅延列挙にすると解放後参照になる）。
- **`FwpmEngineSetOption0` を自己診断から呼ばない。** `FWPM_ENGINE_COLLECT_NET_EVENTS` はシステム全体に効く設定で、CI ランナーは管理者なので UAC も無しに本当に変わり、途中で落ちれば戻らない（`ElevatedNetsh` を呼ばないのと同じ理由）。ユーザーがボタンを押したときだけ立て、**自分で立てたときだけ**終了時に戻す（もともと有効だった環境の設定には触らない）。
- **プロセスが「—」でも壊れていない。** 実機のフラグは `0x011F`（プロトコル・送信元/宛先アドレス・ポート・IP バージョンだけ）で、**アプリ(`0x20`)もユーザー(`0x40`)も一度も立たない**。受信の遮断は通信が届く前に落ちているので、持ち主のアプリが存在せず Windows が付けようがない。「—」＝情報が付いていない、「⚠ 読み取れず」＝付いているが読めなかった、で意味が違う。**この 2 つを一緒にしないこと。**
  - 疑うときはフラグの論理和を見る。`0x20` が一度も立たなければ環境の話で、立つのに 1 件も読めないならこちらの読み方が悪い。この判定は `WfpViewModel.DescribeAppIdGap` に閉じてあり、自己診断が 3 通りの分岐を固定している。
  - オフセットの読み違いを疑う前に**住所とポートが正しいかを見る**。あれが正しく出ているならヘッダの解釈は合っている。
- レイアウト誤りの防波堤は自己診断の**合成バッファ検査**。オフセット表どおりに自前で組み立てたイベントをパーサに食わせるので、実イベントが 0 件の CI でも必ず走る。

### ファイル転送（FTP / SFTP のクライアント）

- **`FtpWebRequest` は使えない。** 廃止済み（SYSLIB0014）で、この repo は
  `TreatWarningsAsErrors=true` なので**ビルドが通らない**。FTP は自前で書いてある。
- **難所は通信ではなく一覧の解釈。** 書式はサーバ任せなので、解釈は
  `Core/Ftp/FtpListing.cs`（UNIX 形式 / DOS 形式 / MLSD）と
  `Core/Ftp/FtpReplyReader.cs`（複数行応答）に押し出して xUnit で固めてある。
  生成側（`FtpFormats`）が既にあるので**往復テスト**が書ける。
  **読めない行は捨てる。例外にしない** — 1 行のために一覧全体を失う方が困る。
- **UNIX 形式の日付には年が無いことがある。** 時刻が出ている＝直近半年以内なので
  未来にはなりえない。今年とみなして未来に飛んだら前年（時計のずれぶんだけ 1 日の猶予）。
- **LIST の語数に 9 を要求しない。** グループ列を出さないサーバがある。7 語から受ける。
- **PASV が返すアドレスを使わない。** NAT の内側のサーバは自分の私設アドレスを答えるので、
  そこへ繋ぐと届かない。**ポートだけもらって、繋ぐ先は制御接続と同じ相手**にする。
- **PORT は実装しない。** こちらが待ち受ける形になり、ファイアウォールと NAT で詰む
  （サーバ側が PORT を持っているのは受ける側だから）。
- **転送は `TYPE I` 固定。ASCII モードを持たない** — 改行を変換され設定ファイルが壊れる。
- **`LingerOption(true, 0)` を使わない。** あれは測定用ソケットの決まりで、
  ファイル転送では最後まで送り切る必要がある（TCP ping の項と混同しないこと）。
- **STOR の終わりはデータ接続を閉じて伝える。** 閉じる前に 226 を待つと止まる。
- SFTP は SSH.NET を包むだけ。**ホスト鍵は受け入れて指紋を控え、画面に出す**
  （`SshConnection.cs` と同じ方針。理由もあちらのコメントに書いてある）。
- **自己診断は自分のサーバへ自分のクライアントで往復させる**（偽の Cisco 機器・
  偽の STUN と同じ手）。loopback のみで外へは 1 バイトも出さない。
  そのために自前の FTP サーバへ `MKD` / `RMD` / `RNFR` / `RNTO` を足してある。
  **`RMD` は空のときだけ許す**（取り消せない操作なので中身ごとは消さない）。
- **`Core.Files.RemotePath` と同じ名前のプロパティを VM に作らない。**
  クラスが隠れて `RemotePath.Combine` が呼べなくなる（画面側は `RemotePathText`）。
- **パスワードは保存しない。** 覚えるのは接続先とユーザー名だけ。窓を開くのは画面の仕事で、
  VM は委譲（`Confirm` / `Ask`）を持つだけ。**結線前の既定は「いいえ」**
  （結線し忘れたときに黙って消えるより、何も起きない方がよい）。

### 投げっぱなしのタスク（`_ = …Async()`）

待受ループ（FTP/TFTP/syslog/Trap）とセッションは `_ = …Async()` で放してある。
**放したタスクから出た例外は誰も観測しない** — ファイナライザスレッド経由の
`UnobservedTaskException` として `crash.log` に落ちるだけで、**プロセスは落ちない**。
だから気づけない。実際に FTP サーバでは「転送のあと停止するたびに毎回」出ていたのに、
CI も自己診断も緑のまま通していた（2026-08-23 にローカルで実行して発覚）。

- **並列数の枠（`SemaphoreSlim`）を `ContinueWith` で外から返さない。** その継続もまた
  放したタスクになるので、`Release` が投げたときの行き場が無い。**取ったら、そのセッション
  自身の `finally` で返す** — セッション側は catch-all を持っていて自分では投げずに終われるので、
  `_ = RunSessionAsync(…)` だけなら未観測の例外は生じない。
- **`Dispose()` で `SemaphoreSlim` を破棄しない。** `Stop()` は待受を畳むだけで、進行中の
  セッションはその後も動いており、終わってから枠を返しに来る。破棄するとその `Release` が
  `ObjectDisposedException` になる（これが上の不具合の正体）。`AvailableWaitHandle` を
  使わない限り解放すべき資源は持っていないので、破棄しないのが正しい。
  **在席セッションを待つ形にもできない** — `Dispose()` は UI スレッドから同期で呼ばれる
  （`FileServerViewModel.Stop`）ので、待つと窓が固まる。
- **`WaitAsync(0, token)` にトークンを渡さない。** 0 ミリ秒待ちは決して待たないのでトークンは
  要らず、渡すと停止と競った瞬間に `OperationCanceledException` がループの外＝放したタスクへ出る。
  `Wait(0)` で足りる。
- **セッションの組み立ても `try` の中に入れる。** ソケットの生成や `RemoteEndPoint` の読み取りは
  投げうる位置で、そこで抜けると枠が返らないまま上限が減り続ける（やがて誰も繋げなくなる）。
- 防波堤は **`CrashLog.Count` を見る自己診断**と、**CI の `crash.log` 判定**の二重。
  ここが無いと、この種の不具合は何も壊さずに静かに残り続ける（→「ビルド後の検証」）。

### ビルド後（publish 後）の検証

**終了コードだけを見ていると、プロセスを落とさない不具合は永久に見つからない。**
握り潰した例外（`MainWindow.OnClosing` の catch-all）と、捨てたタスクからの
`UnobservedTaskException` がそれで、どちらも `crash.log` にしか現れない。
実際に FTP サーバの不具合を長く緑のまま通していた。post-build の検証は 3 つで見る:

- **`--selftest` は `CrashLog.Count` を検査ごとに見る。** 検査の前後で増えていたらその検査を失敗にする。
  ファイルの有無を CI で見るのと違い、**どの検査が原因かが分かる**。
  非同期に上がる例外は次の検査が濡れ衣を着るので、**サーバを止めた直後の検査は
  自分の中で `SettleTasks()` を呼んで確定させる**（GC → ファイナライザ → 例外、の 3 段を少し粘って待つ）。
  最後に全体を 1 度確定させる項目も置いてある（保険）。
- **GUI スモークは強制終了しない。** `CloseMainWindow()` で正規の終了経路（`OnClosing`）を通し、
  **終了コードと `crash.log` の両方**を見る。`Stop-Process -Force` に戻すと、
  5 つの待受の後始末・WFP の記録設定の復帰・**Windows のプロキシ設定の復帰**が丸ごと未検証になる。
  **`OnClosing` に確認ダイアログを足すと CI がそこで止まる。**
- **待受は「立てたまま閉じる」まで見る。** 起動と停止だけでは、現場で普通に起きる
  組み合わせ（サーバを立てっぱなしでアプリを閉じる）を通らない。これがまさに
  FTP の不具合の発火条件だった。
- **自前のサーバには自前のクライアントで往復させる**（FTP / TFTP / SFTP / syslog / Trap）。
  そのために要る**組み立て側は Core に置く** — `TftpPacket.Request` と `SnmpCodec.BuildTrapV2` は
  製品では使わないが、Core にあれば xUnit で読み取り側と往復させて固められる。

### syslog / Trap の受信

- **`FileServerEvent` は FTP/TFTP/SFTP/syslog/Trap の共通口。** syslog の重大度はここに `Severity` として通す（持たないものは -1）。かつては入れ物が無く、`SyslogParser` が解釈した重大度を「`[warning] 本文`」という 1 本の文字列に潰して捨てていた。**解釈済みの値を文字列へ埋め戻さないこと** — 絞り込みも色分けもできなくなる。
- **重大度は 0=emerg 〜 7=debug で小さいほど重い。**「err 以上」は「3 以下」を意味する。絞り込みの実装で向きを間違えやすい。
- 一覧の絞り込みは基底 `FileServerViewModel` に置く（5 画面が一度に得をする）。受信した全行を `_all` に持ち、画面の `Log` はそれを通した写し。**届いた 1 行だけ足す**（毎回作り直すと重い）。
- **varbind を件数で打ち切らない。** 機器によっては肝心の中身が 7 個目以降に来る。行が長いぶんは画面側で省略し、全文は `ToolTip` に出す。

### 一覧の CSV 書き出し

- 保存は `Services/CsvExport.cs` の 1 か所。**BOM 付き UTF-8**（無いと日本語版 Excel が化ける）。取り消されたら `null` を返すので、呼ぶ側は `if (... is { } message) Status = message;` と書く。
- ファイル名は `DeviceReport.Sanitize` を通す。宛先を混ぜる画面（経路の `trace-<宛先>`）があり、IPv6 を入れられるとコロンだらけになる。**`Path.GetInvalidFileNameChars()` は使わない**（Linux では `/` と NUL しか返さず、開発機と CI で結果が変わる）。

### Excel ブック（.xlsx）の書き出し

- **ライブラリを足していない。** xlsx は zip に XML を数枚入れただけのもので、必要なのは 5 パーツ（`[Content_Types].xml` / `_rels/.rels` / `xl/workbook.xml` / `xl/_rels/workbook.xml.rels` / `xl/worksheets/sheet1.xml`）。標準の `ZipArchive` で足りる。**ClosedXML / EPPlus を再提案しないこと**（単一ファイル発行との相性をローカルで確かめられず、EPPlus はライセンスの都合もある）。生成は `Core/Reporting/XlsxWriter.cs` の 1 か所。
- **シートの要素は順番が決まっている**（`dimension` → `sheetViews` → `sheetData` → `autoFilter`）。入れ替えると Excel は「読めない内容」と言って**ファイルごと開かない**。
- **文字列は `inlineStr` で書く。** `sharedStrings.xml` が要らず、**数式として評価されない**ので CSV のような無害化（`'` を足す）も不要。逆に、数値として入れたいものは `<v>` で書く — 文字列のままだと並べ替えも数値の絞り込みも文字列として振る舞う。
- **数値とみなす判定は狭く取る。** `007` を `7` にすると戻せない。先頭 0・16 桁以上・区切りが 2 つ以上（IP / MAC）は文字列のまま（`XlsxWriter.IsPlainNumber`）。
- **制御文字は落とす。** 機器の出力には混じることがあり、1 文字通すだけで Excel はファイルごと開けなくなる。
- **見た目は CI でも確かめられない**（ランナーに Excel は無い）。xUnit が守れるのは zip の中身と XML の形まで。ここを触ったら**実機で 1 度開いて確認する**こと。

### 試験タブ（業務確認試験）

- **プロキシは `HttpClientHandler.Proxy` を明示して作り分ける。Windows の設定は書き換えない。** システム設定は**プロセス起動時に読まれて固定される**ので、切り替えながら試すこと自体ができない。書き換える方式はほかのアプリを巻き込み、戻し忘れの事故にもなる。
- **PAC は WinHTTP に評価させる。** .NET の `WebProxy` は固定アドレスしか扱えず `FindProxyForURL` を実行できない。`Interop/WinHttpNativeMethods.cs` が `WinHttpGetProxyForUrl` に**指定した PAC の URL**を渡して評価する（システム設定には触らない）。**PAC は宛先ごとに答えが変わるので、試験する URL ごとに引き直す。** 返ってきた文字列は `GlobalFree` で解放する。
- **応答コードだけで合格にしない。** フィルタリング製品（Zscaler・i-FILTER など）は遮断時に**自前のページを HTTP 200 で返す**。`Core/Verify/HttpVerdict.cs` が本文も見る。文言の一覧は当てにならないので、**確実にしたい項目は「期待する文字列」を書いてもらう**のが正しい使い方。証跡には最終 URL と Server ヘッダを必ず残す。
- **証明書の検証を切らない。** TLS を傍受するプロキシを使う環境では、その CA が Windows に入っていることが前提。切ると「見えているのに実は別物」を通してしまう。
- **プロキシが効くのは HTTP の項目だけ。** TCP・メール・DNS・Teams は直接出るので、プロキシを何本選んでも 1 回しか実行しない。画面にもその断りを出している（`UsesProxy` の分岐はテストで固めてある）。
- **Teams の名前解決は合否に使わない**（2026-08-19 ユーザー指示）。プロキシ環境では宛先の名前を引くのは
  プロキシの仕事で、この PC で引けないのが普通。引けないだけで不合格にすると、**実際に Teams が使える環境が落ちる**。
  引けたかどうかは結果に書き添えるだけにして、判定は HTTPS と UDP で行う（本当に届いていなければそこで落ちる）。
- **UDP を 1 バイトも送れていないときは不合格にしない**（2026-08-19 ユーザー指示。実際に通話できているのに
  「応答がありません」と出た）。リレーの名前はプロキシ環境では引けないことがあり、UDP はプロキシを通らないので
  **問い合わせ自体が出ていない**。`StunOutcome.Sent` で「送れたか」を持ち、送れていなければ **△ 注意**（確かめられなかった）にする。
  **ポートごとの理由（`StunOutcome.Problem`）を捨てない** — まとめて「応答がありません」と書くと切り分けができない。
- **リレーの名前が引けないときは HTTPS 経由（DoH）で引き直す**（2026-08-19 ユーザー指示）。
  社内 DNS が `SERVFAIL` を返し、外向きの DNS（8.8.8.8 など）へは**経路も無い**環境が実際にあった。
  **HTTPS はプロキシを通る**ので、`dns.google` の JSON の口に試験中のプロキシで問い合わせれば
  アドレスだけは得られる。**素の DNS（UDP 53）を代わりに使わない**（経路が無い）。
  引くのは**この PC で引けなかったときだけ**。組み立てと読み取りは Core（`DohLookup`）に置いて CI で固める。
  **引けたアドレスは試験 1 回の全部の行で使い回す**（`RelayAddresses`）。UDP はプロキシを通らないので
  投げる先はどの行でも同じで、**1 本のプロキシで引ければ十分**。選んでいるプロキシで塞がれていたら
  ほかのプロキシでも試す（片方だけ DoH を塞ぐ環境が実際にあった）。
- **Teams の UDP はただ投げても判定できない。** 無応答が「開いている」のか「塞がれている」のか区別できないため、**応答が返る STUN** を使う。`Core/Verify/StunMessage.cs` はバイト列の組み立てと解析だけなので CI で固められ、自己診断は **loopback の偽 STUN サーバ**と往復する（偽の Cisco 機器と同じ手）。
- **目視の項目は、1 項目だけの実行でもブラウザ待ち行列を通す**（`RunOneAsync` が
  `StartBrowserQueue` に 1 件だけ積む）。CheckRunner へ直に流すと WinINET を切り替えずに
  開いてしまい、「選んだプロキシで見ていない」証跡になる（2026-08-21 報告）。
- **目視の項目はプロキシ 1 本につきまとめて開く**（2026-08-19 ユーザー指示）。ブラウザは Windows の設定に従うので、
  切り替えは 1 本につき 1 回。**そのプロキシぶんの ○ / ✕ が全部付いてから**次へ進む。
  **戻すのは 4 か所** — 全部終わったとき・中断したとき・失敗したとき・アプリを閉じるとき
  （目視の判定は人待ちなので、閉じられた瞬間に切り替わったままになる道がある）。
- **タブを開いただけでは外へ出ない。** Meraki と同じ理由（CI の Windows ランナーは外に出られるので、開いた瞬間に本番へ試験が飛ぶ作りにしない）。
- 認証は統合 Windows 認証（`UseDefaultCredentials` と `DefaultProxyCredentials`）。**認証情報は保存しない**（`AppSettingsDocument` に入れ物を作らない）。

### 外部 API（Meraki）

- **別ホストへリダイレクトされると `Authorization` ヘッダが落ちる。** Meraki は組織によってリージョン別ホスト（api.meraki.cn など）へ 302 で誘導するので、`AllowAutoRedirect` を既定（true）のままにすると**追従はするが鍵が消えて 401 になる**。`MerakiDashboard` は `AllowAutoRedirect=false` にして自分で `Location` を追い、ヘッダを付け直している。自動追従に戻さないこと。
- **API キーはどこにも保存しない・出さない。** settings.json にも書かず（毎回入力）、`Status` の文言・`CrashLog`・URL クエリにも載せない。入力欄は `PasswordBox`（TextBox にすると **F12 の画面 PNG 保存にキーが焼き込まれる**）。エラーメッセージは応答本文を読まず、ステータスコードから組み立てる。
- **clients の `vlan` は数値と文字列が混在する。** `int?` で受けると `JsonException` で一覧が丸ごと落ちるので `JsonElement` で受けて表示側で文字列にする。Meraki は項目の増減もあるため POCO を作らず `JsonDocument` で必要な項目だけ拾っている。
- **selftest から実 API を叩かない。** タブを選んだだけでは通信しない作り（`OnActivated` を持たない）にしてある。CI の Windows ランナーは外に出られるので、タブ選択で取得を始める作りにすると全タブ検査から本番へリクエストが飛ぶ。
- ページングは `Link` ヘッダの `rel=next` を追う。20 ページで打ち切り、**打ち切ったことは必ず画面に出す**（黙って切ると「その機器は無い」と誤読される）。429 は `Retry-After` を 1〜60 秒に丸めて最大 3 回まで再送。
- **イベント（`/networks/{id}/events`）は 1 ページだけ取る**（`GetOnePageAsync`）。この応答には**常に次ページの Link が付く**ので `GetPagesAsync` に渡すと 20 ページぶん引く。**応答も配列ではなく `{pageStartAt, pageEndAt, events:[…]}` のオブジェクト**なので、ほかの一覧と同じ `Items` では 1 件も読めない。
- **MX の LAN/WAN ポートの速度・全二重はダッシュボード API に無い**（画面の Appliance status にしかない）。MS は `/devices/{serial}/switch/ports/statuses` で `speed`（「1 Gbps」）と `duplex` が取れる。**無いものを推測で埋めない** — 導入時確認では MX のぶんを「目視で確認」の行として人に渡している。
- **どの WAN を使う設定かは `/devices/{serial}/appliance/uplinks/settings` にしかない。** 状態（`appliance/uplink/statuses`）には使っていない WAN2 も「not connected」で出てくるので、状態だけで「全部リンクアップ」を判定すると**単線の拠点が必ず不合格になる**。
- **`status` は `active` と `ready` が両方とも「リンクアップ」**（ready は冗長側の待機）。`connecting` はまだリンクアップしていない。表示用の文字（`◌ 待機` / `◌ 接続中`）では区別が付かないので、`MerakiUplinkRow.RawStatus` に生の値を持たせてある。
- **MX の LAN IP は組織まとめの応答に無い。** `organizations/{id}/devices` にも `devices/statuses` にも入っておらず（あちらの `lanIp` はスイッチや AP の管理アドレス）、拠点ごとに `networks/{id}/appliance/vlans` を引く。**VLAN を使っていない拠点は 400 が返る**ので `appliance/singleLan` へ落とす（**応答は前者が配列・後者がオブジェクト 1 つ**）。取れない拠点は空欄のままにする。
- **拠点のセグメントは VLAN で切ってあるのが普通。** スタティックルートはその先に別のルータがある拠点にしか無いので、ルートだけを見ると**大半の拠点が空欄**になる（2026-08-17 に実機で発覚）。VLAN のサブネットとルートの和で見る。
- **`firmware` には版が入らないことがある。** 設定と違う版で動いている機器には **`Not running configured version` という英文が版の代わりに返る**。実際の版は `organizations/{id}/firmware/upgrades/byDevice` の**完了した更新の `toVersion`** から補う（進行中・取り消しは信用しない）。補えないときは版を騙らず「⚠ 設定と違う版」とだけ出す。
- **通信量のタブは削除した**（2026-08-18 ユーザー指示。`appliance/uplinks/usage/byNetwork` の値がダッシュボードと桁で合わず、信頼できなかった）。**再提案しないこと。**
- **停止している機器の回線は一覧に出さない**（切れているのが分かりきっているため。2026-08-17 ユーザー指示）。**導入時確認だけは落とさない** — あちらは「取れなかった」と「切れている」を区別する必要があるので、VM が絞り込み前の一覧（`_allUplinks`）を持っている。
- **全拠点ぶんの取得は打ち切らない**（2026-08-17 ユーザー指示。以前は 40 拠点で打ち切っていた）。代わりに**始める前に待ち時間の断りを出し、取り終わったら消す**（`SlowNotice` / `ClearSlowNotice`）。

### 外部 API（Cisco ACI / APIC）

- **応答はクラスが何であっても 1 つの形しかない。** `{"totalCount":"3","imdata":[{"<クラス>":{"attributes":{…},"children":[…]}}]}`。**クラスごとに型を作らない** — `Core/Fabric/AciMo.cs` の 1 種に読み替えて、行は `AciCatalog` が組む。属性は版で増減するので、決め打ちにすると 1 項目の想定外で一覧が丸ごと落ちる（Meraki と同じ判断）。**属性が無ければ空文字。例外にしない。**
- **`totalCount` は数値ではなく文字列で返る**（`"3"`）。数値として受けると版が変わった瞬間に読めなくなる。
- **DN は素の `Split('/')` で割ってはいけない。** `pathep-[eth1/1]` のように**角かっこの中に区切り文字が入る**ので、`eth1` と `1]` に壊れる。`AciDn.Segments` が角かっこの深さを見ながら切っている。vPC の `protpaths-103-104` は 2 台に跨がるので「103-104」のまま出す（どちらか片方に決められない）。
- **`CookieContainer` に任せない。** `aaaLogin` の応答本文から `token` を読み、毎回自分で `Cookie: APIC-cookie=…` を付ける。`CookieContainer` は `HttpClientHandler` の持ち物なので、**自己診断が偽のハンドラを挿すとその経路が素通り**してしまう（偽 APIC との往復検査が意味を失う）。
- **証明書の検証を切らない。** APIC は自己署名が普通だが、`ServerCertificateCustomValidationCallback` で**受け入れ済みの指紋と一致したときだけ**通す（SSH のホスト鍵と同じ思想）。
  - **握手のコールバックの中で人に聞かない。** UI スレッドと握手スレッドで詰む。①コールバックは判定するだけで、**断ったときも指紋を控える**（出せなければ人は受け入れようがない）②VM が失敗を受けて、UI スレッドで `ConfirmFingerprint` を出す③受け入れられたら `settings.json` に足して**もう 1 度だけ**やり直す。**繰り返さない**（認証の失敗を繰り返すとアカウントが固まる）。
  - コールバックはリクエストのスレッドから呼ばれるので、指紋を持つフィールドは `volatile`。
  - **指紋が変わったら黙って上書きしない。**文面を変えてもう一度聞く（相手が入れ替わっている可能性がある）。
- **`rsp-subtree-include=health` に `required` を付けない。** あれは「ヘルスを持つものだけ返す」という意味で、ヘルスの付いていないクラスに使うと**1 件も返らない**。`fabricNode` に付けてノードが 1 台も出ず、ポートの画面が丸ごと使えなくなっていた（2026-08-17 に実機で発覚）。**持っていないものは「—」で出す**方がよい。
- **ノードは `fabricNode` ではなく `topSystem` から取る。** スイッチのヘルスは `fabricNode` ではなく `sys`（＝`topSystem`）にぶら下がっており、`topSystem` の dn は**そのままポートの絞り込みに使える形**（`topology/pod-1/node-101/sys`）でもある。`fabricNode` は型番・シリアル・版を持つので構成の一覧はあちらで引く。
- **`l1PhysIf` をファブリック全体で引かない**（数万行）。必ずノードで絞る（`ClassPath` の `scopeDn`）。
- `rsp-subtree=children`（EPG・エンドポイント・ポート）と `rsp-subtree-include=health`（ヘルス）は**別物**。用途を混ぜない。
- **フォールトは既定で info を除く**（`AciCatalog.FaultFilter`）。入れると数千行になり、重いものが埋もれる。重大度の選択欄は作らない — 既定を正しくする方が早い。
- ページングは `page` / `page-size` と `totalCount`。**20 ページで打ち切り、打ち切ったことは必ず画面に出す**（Meraki と同じ。黙って切ると「その機器は無い」と誤読される）。
- **設定の書き出しには `rsp-prop-include=config-only` を必ず付ける**（`AciCatalog.TenantExportPath`）。付けないと稼働値（カウンタ・時刻・状態）まで混ざり、**設定を何も変えていなくても差分だらけになる**（`show ip route` の経過時間と同じ問題）。
  - **JSON のまま差分にしない。** APIC は子の並び順を約束しないので、同じ設定でも並びが変わるだけで差分になる。`AciConfigExport` が並べ替えて字下げした行に直す。**「同じ設定なら同じ文字になる」ことを xUnit が固定している。**
    - **並べ替えの鍵は「その枝を描いた文字そのもの」。** かつてのクラス→dn→名前では、dn を持たない子のうち name も持たないクラス（`fvRsPathAtt` は tDn、`fvSubnet` は ip が識別子）が全部同点になり、安定ソートが APIC の返した順を残して**並びの入れ替わりが差分に出た**（2026-08-23 実機報告）。識別子がどの属性かをクラスごとに知る方式に戻さないこと — 知らないクラスで必ず同じ穴が開く。
    - **作業前後の欄は手で書き換えられる**（2026-08-23 ユーザー指示）。「作業前を保存」で残した控えを貼って（ファイルの放り込みも受けて）今と比べる使い方のため。読み取り専用に戻さない（自己診断が固定している）。書き換えた側の見出しには「手で編集」と出す — 取得時の行数を名乗ったままだと証跡が嘘になる。
  - 版によっては `modTs` / `uid` / `lcOwn` などが混じる。落とす一覧は `AciConfigExport.VolatileAttributes` の 1 か所で、**実機で余計な差分が出たら 1 行足せば直る**。
  - **枝を丸ごと返す問い合わせなので、ページングを重ねない**（`PagePath` を足さない）。
  - **見比べは ACI タブの中で完結させる**（「差分比較」サブタブ）。差分比較タブへ送る形は
    「分かりづらい」と指摘された（2026-08-17）。左右の行のテンプレートは `Window.Resources` の
    `DiffRow.Left` / `DiffRow.Right` を共用し、縦スクロールを合わせる相手は `Tag` で持たせる
    （画面が増えても code-behind に対応表を持たない）。
  - **スナップショット（Config Rollback）は作らない。**作成は POST ＝書き込みで、読み取り専用の縛りから外れる。既存の一覧は構成タブの「設定バックアップ」で見える。
  - **設定のバックアップ機能は持たない**（2026-08-18 に一度作り、同日ユーザー指示で削除した）。
    `uni` の枝を `config-only` で GET して落とすところまでは作れるが、それは
    **APIC の設定エクスポート（tar.gz）とは別物**で、Import で戻せず、secure property も入らない。
    同じものを得る道は 3 つとも難がある — ①この PC の FTP/SFTP で受ける（APIC 側の手作業が要る）
    ②POST でエクスポートを作らせる（読み取り専用の縛りを外す）③既にあるものを落とす
    （新しく取れず、取得 URL は実機でしか確かめられない）。**再提案しないこと。**
  - **差分比較で取った「作業前」「作業後」はファイルに残せる**（2026-08-18 ユーザー指示。作業の証跡用）。
    画面に出しているものと同じ整形済みの文字をそのまま書くだけで、取り直しはしない。
- **読み取り専用。**POST するのは `aaaLogin` / `aaaRefresh` / `aaaLogout` の 3 つだけ。フォールトの確認応答も設定変更も作らない（本番ファブリックを丸ごと握っているので、書ける口があること自体が事故の入口）。
- **パスワードは保存しない・入れ物も作らない。** `AppSettingsDocument` に持つのは接続先・ユーザー名・**受け入れた指紋**だけ（指紋は秘密ではない。SSH の known_hosts と同じ）。`HandoverPanels` には何も足さない。
- **`aaaRefresh` にタイマーを持たせない。** 要求の直前に寿命を見るだけ（押したときだけ通信する画面なので、背景で回す理由が無い）。
- **状態の種別は `AciSeverityKind`（Core/Fabric）で持つ。** 接続タブの `ConnectionStateKind` には「危険」が無く、足すと既存 3 タブの分岐に手が入る。色は既存の Ok / Warn / Error / Info のブラシ対で足りる（**新しい色キーを増やさない**＝明暗パレットの突き合わせ検査に触らない）。
- **自己診断から実 APIC を叩かない**（Meraki と同じ。CI ランナーは外に出られる）。`OnActivated` を持たせないこと。検査は偽ハンドラ（`SelfTest.FakeApic`）との往復で行う。
- **機器の台帳は `fabricNode`。** 型番・シリアル・版はここにしか無い（`topSystem` はヘルスとポートの絞り込み向け。用途を混ぜない）。2026-08-17 にユーザー指示で「構成」タブ（4 種別）を畳み、`fabricNode` の**機器一覧**タブへ置き換えた。テナントやポリシーの一覧は再提案しないこと。
- **ノードと口は数字で並べる。** 文字のままだと 101 が 9 より前に、eth1/10 が eth1/2 より前に来る。並べ替えの鍵は `AciCatalog.PortSortKey`（数字を 6 桁に揃える）1 か所に閉じてあり、**見出しを押したときの並べ替えもこの鍵を使う**（`MainWindow` の列名の対応表で `InterfaceKey` / `NodeKey` を指す）。
- **ポートの説明（`l1PhysIf.descr`）は出す。** 口だけでは何が刺さっているか分からない。
- **「応答を表示」ボタンを外さない。** 属性名の思い違いは実機でしか分からず、これが無いと「表が空」から先へ進めない。

### Cisco WLC / Catalyst 9800

- **WLC タブは削除した**（2026-08-18 ユーザー指示。「思ったように動かない」ため）。
  `WlcViewModel` / `WlcCatalog` / `WlcShow` と、その設定・列幅・ⓘ の説明ごと消してある。
  RESTCONF の実装はそれより前に消してある。**再提案しないこと。**

### タブの入れ子（その他）

- **`TabItem` の中身は、視覚ツリーで `TabItem` の下に無い。** 選ばれたタブの中身は
  **親 `TabControl` の `ContentPresenter`** の下に置かれるので、内側のタブから
  `VisualTreeHelper.GetParent` で遡っても外側の `TabItem` を素通りする。これを踏むと
  ①**右クリックからの遷移で親タブが開かず画面が変わらない**（`Show`）
  ②**閉じたタブが「見えている」ことになって OS を叩き続ける**（`IsShowing`）
  ③**自己診断が内側のタブを 1 枚も実体化できず、規約の検査が丸ごと素通りする**。
  先祖をたどる判定は **論理ツリー**（`LogicalTreeHelper.GetParent`）で行う。
  数を数えるだけなら論理ツリーの方が確実（実体化を待たなくてよい）。
  実体化して視覚ツリーを見たいときは、**`TabItem` ではなく親 `TabControl` を起点**にする。
- **`TabItem.IsSelected` は「その TabControl の中で選ばれているか」しか表さない。**
  内側の `TabControl` は生成時に先頭の子を自動で選ぶので、親を一度も開いていなくても
  内側の 1 枚目は true。判定は `MainWindow.IsShowing` 1 か所に閉じてある。
- **入れ子は 2 段まで。** 3 段にすると道具を探せなくなる。
- **まとめたタブは「その他」1 枚**（2026-08-16 ユーザー指示。調べる / NW機器 / 受ける を畳んだ）。
  **Meraki は 2026-08-17 にユーザー指示で「業務確認」の右隣の主タブへ出した**。
  **主タブの並びは Ping / WFP / 差分比較 / IP設定 / 業務確認 / Wi-Fi / Meraki / その他**
  （2026-08-18 ユーザー指示。よく使う順）。並べ替えは XAML の `TabItem` を動かすだけで、
  コードは順番に依存していない（自己診断もタブを名前で引いている）。
  中身 16 枚の**分類は各 `TabItem` の `Tag`**（`調べる` / `NW機器` / `受ける`）で持つ。
  同じ分類は**ひと続きに並べる** — 飛び飛びだとメニューに同じ見出しが 2 度出る。
  付け忘れと並びの乱れは自己診断が捕まえる。
- **束ねた中の帯は出さない。** 1 枚だけ出しても行を 1 つ食うので、いま開いているものは
  **「その他」の見出しの右隣**に出す（`MainWindow.UpdateOtherHeader`。2026-08-17 ユーザー指示）。
  `ItemContainerStyle` は `Collapsed` のまま（トリガーで出し直さない）。
  **見出しの件数は数えて入れる** — 直書きだと中身を増減したときに黙ってずれる。
  **`TabControl.Resources` に暗黙スタイルとして置くと、ACI の中のサブタブまで
  巻き添えで隠れる**（リソースは子孫全体に効く。`ItemContainerStyle` はその
  `TabControl` 自身の項目にしか効かない）。見た目を主タブに揃えるため
  `BasedOn="{StaticResource {x:Type TabItem}}"` を必ず付ける。
- **「その他」のドロップダウンは `OpenOtherTabsMenu` の 1 本**（見出しクリックから。
  動的な子項目は MainMenu 配下でないためアクセスキー検査の対象外 — 矢印キーで辿る）。
  **表示メニューからの同じ動線（その他の画面(_O)）は 2026-08-20 に足して同日ユーザー指示で
  削除した。再提案しない。**メニュー帯右端の Monitor.StatusMessage の常駐表示も同日削除
  （IdleHint が帯に居座って煩い。進行状態は Ping/TCP タブの中に出す）。
- **見出しを押すとメニューが降りてくる**（`OnMainTabsMouseDown`）。**降ろすのは「その他」だけ** —
  「中に切り替えを持つタブ」で拾うと、サブタブを見せている主タブ（Meraki）を
  押してもメニューが出るだけで画面が変わらない（2026-08-17 に実機で指摘された）。
  分類の変わり目に
  押せない見出し（`IsEnabled=false` の `MenuItem`）を挟む。
  **メニューを出すだけでは画面を変えない** — 項目を選んで初めて切り替える
  （見に行っただけのつもりで、測定中の画面から飛ばされないように）。
  切り替えるときは**中身を先に選んでから親を開く**（逆順だと親の切り替えで戻される）。
- **まとめたタブの見出しには件数と `▾` を出す**（`その他　16 ▾`）。数字は XAML の直書きなので
  中身を増減すると黙ってずれる。自己診断が実際の本数と突き合わせている。
- **どのタブにも先頭に 1 行の説明と `ⓘ`（ホバーで詳しい説明）を置く。** タブ名だけでは
  何の画面か伝わらない。足し忘れは自己診断が捕まえる。
  **例外は「その他」**（2026-08-16 ユーザー指示）— 中身を選ぶだけの入れ物で、
  説明する内容が無い。自己診断も `OtherTab` を除外している。
- **1 行の説明は「その画面で何ができるか」を一言で**（全角 10〜50 字。自己診断が幅で見る）。
  細かい話は `ⓘ` に置く。**一度 100 文字を超える説明を書いて「細かすぎて分かりづらい」と
  指摘された** — 1 行の役目は「何ができるか」であって、事情の説明ではない。
- **Catalyst Center のタブは削除した**（2026-08-18 ユーザー指示。2.3.5 系では端末の一覧も無く、できることが限られていたため）。`DnacClient` / `DnacCatalog` / `DnacJson` ごと消してある。**再提案しないこと。**
- **`ⓘ` の説明は `App/Help/TabHelp.cs` に置く。** XAML の属性値だと改行が入れられず
  （`&#10;` を並べることになる）、書くのもレビューするのも成り立たない。
  画面からは `{x:Static help:TabHelp.Ping}` で引く。書き方の決まりは 3 つで、
  **自己診断が実際に描かれた ToolTip を読んで守っている**:
  ①**1 行目はタブ名**（浮いた箱だけ見えている状態で何の説明か分かるように。
  入れ子の中は「Meraki ─ ネットワーク」のように親を添えてよい）
  ②**節と `・` の箇条書き**で書く（節は「この画面ですること」「知っておくとよいこと」
  「気をつけること」「できる操作」から必要なものだけ）。地の文を続けない
  ③**1 行は全角 68 字まで**（`ToolTip` の `MaxWidth` 840px ぶん）。**折り返しを当てにしない** —
  折り返すと箇条書きの頭が揃わず、かえって読みにくい。収まらないなら 2 行に割り、
  続きは全角スペースで下げる。幅は文字数ではなく `Core/Reporting/TextWidth.Of` で数える
  （`.Length` だと日本語と URL で基準が変わる）。
  もとは 145 文字（最長 317）の地の文 1 段落で、**ホバーしている間に読み切れなかった**。
- **ツールチップは読ませる文章が入る前提の体裁にしてある**（`Controls.xaml` の暗黙スタイル）。
  本文サイズ 12px・行間 19px・幅 840px・余白 12,9。狭くすると箇条書きが数語ごとに折り返し、かえってぶつ切りに見える（1 度 420px にして指摘された）。`LineHeight` は
  `LineStackingStrategy="BlockLineHeight"` と対で書かないと効かないことがある。
- **記録の画面は持たない**（2026-08-16 にユーザー指示で畳んだ）。書き出しはファイルメニュー。
  `RelayCommand` は `CommandManager` に乗っていないので、**メニューを開くたびに
  `ReportViewModel.RefreshSaveCommands()` を呼ぶ**こと。呼ばないと起動直後の
  「まだ何も無い」判定のまま固まり、保存が押せなくなる。
- **画面から VM を呼ぶときの初期化順に気をつける。** `SelectedTemplate` の setter が
  `DeleteTemplateCommand.RaiseCanExecuteChanged()` を呼ぶので、**コマンドを作る前に
  ひな型を組み立てると生成時に `NullReferenceException` で落ちる**（実際に踏んだ）。
  コンストラクタは「コマンド → 状態」の順で書く。

### 測定の開始と停止

- **停止処理を返す前に、必ず 1 度は呼び出し元へ制御を返す**（`StopCoreAsync` の先頭の `await Task.Yield()`）。
  ここを同期で走り切ると、`finally` の `_stopTask = null` が呼び出し元の
  `_stopTask = StopCoreAsync()` より先に動き、**完了済みの停止処理が `_stopTask` に残る**。
  次に停止を押すと `StopAsync()` がそれをそのまま返して素通りし、
  **「停止を押しても止まらない」**になる（実際にユーザーが踏んだ）。
  停止が同期で終わるのは珍しくない（宛先 0 件、ループが既に畳まれている）ので必ず起きうる道。
- **宛先リストは名前を付けていくつも残せる**（2026-08-17 ユーザー指示。Ping と TCP で別の入れ物）。
  **選び直す前に、いま出ている中身を元の名前へ書き戻す**（編集を黙って捨てない）。
  「消す」はリストの名前だけで、**測っている宛先は残す**。
- **開始と停止の入り口は Ping タブと F5 / Shift+F5 だけ**（2026-08-16 にメニューバーからも
  ヘッダからも外した）。ショートカットの実体は `InputBindings`（メニューの `InputGestureText` は飾り）。

### 同梱物のライセンス表示

- **`THIRD-PARTY-NOTICES.txt` に著作権表示とライセンス本文を置く。** exe に埋め込み
  （csproj の `EmbeddedResource` + `LogicalName`）、配布物にもテキストで添える。
  MIT / Apache-2.0 / OFL 1.1 は**いずれも再配布時の添付を条件にしている**ので、
  書き忘れはライセンス違反そのもの。
- **パッケージを足したら通知にも足す。** 忘れてもビルドも自己診断も通ってしまうので、
  xUnit（`ThirdPartyNoticesTests`）が csproj の `PackageReference` と突き合わせている。
- 著作権表示は**推測で書かない**。NuGet の `.nuspec` の `<copyright>` を見る
  （`https://api.nuget.org/v3-flatcontainer/<id>/<version>/<id>.<version>.nupkg` を落として展開）。
  フォントは OTF の `name` テーブル（nameID 0 / 13 / 14）から読める。

### ファイルの読み込み

- **読み込み口はすべてドラッグ＆ドロップに対応させる**（`Services/DroppedText.cs`）。
  `TextBox` は素で「文字のドラッグ」を扱うので、**`PreviewDragOver` で自分の答えを返さないと
  既定の動きに先を越される**。`AllowDrop="True"` だけでは足りない。
- **文字コードは決め打ちしない。** UTF-8 として<b>厳密に</b>読んでみて（`throwOnInvalidBytes`）、
  通らなければ cp932 に落とす。この順で見るのは、UTF-8 のバイト列が cp932 として
  妥当なことはあってもその逆はまず無いため。`throwOnInvalidBytes` を外すと
  **化けたまま「読めた」ことになる**。
- **読み込みは置き換え。** 足す形にすると、前の内容が混ざったまま気づかず誤った差分を見る。
- **管理者として実行中はエクスプローラーからのドロップが届かない**（UIPI。DragOver すら来ず、
  カーソルが 🚫 のまま）。アプリでは直せないので、`AllowDrop` の欄には起動時にツールチップで
  断りを足す（`MainWindow.MarkBlockedDropTargets`。CI ランナーは管理者なので自己診断が
  断りの有無を実経路で検査する）。新しいドロップ口は `AllowDrop` を付ければ自動で対象になる。

### UI

- **`Style` の `Setter` の中に、イベントハンドラを持つ要素を置かない。** `<Setter Property="ContextMenu">` の下に `Click="..."` 付きの `MenuItem` を書くと、**ビルドは通り、起動した瞬間に `XamlParseException: Set connectionId threw an exception` で落ちる**（`MenuItem` を `Grid` にキャストできない、のように無関係な型が出る）。リソースは後から展開されるのに、結線の番号は文書順で振られるためずれる。`ControlTemplate` / `DataTemplate` の中は独自の結線を持つので安全。**メニューはテンプレートの中の要素に付ける**こと。
- **既定スタイルを持つコントロールは、色を継承してくれない。** `ListBox` / `ListView` の既定スタイルは `Foreground` にシステム色（＝黒）を入れるため、暗い配色にしても行の中の `TextBlock` だけ黒く残る（`ItemsControl` は既定スタイルを持たないので継承される）。暗黙スタイルで上書きすること。**キー付きスタイルは暗黙スタイルを継承しない**ので、`BasedOn="{StaticResource {x:Type ListBox}}"` を明示する。`ContextMenu` / `MenuItem` / `ToolTip` も同様にシステム色で描かれる。
- **`TargetListBox` スタイルは Ping 用の `ItemTemplate` を既定で当ててくる。** 型別の暗黙 DataTemplate で出し分ける一覧(接続タブ)にこのスタイルを使うときは、`ItemTemplate="{x:Null}"` で明示的に外すこと。外し忘れるとスタイル側のテンプレートが暗黙テンプレートより優先され、**全行が Ping の「◌ 待機」バッジで描かれる**(バインドは静かに失敗するのでビルドも selftest も通ってしまう。実機で発覚した)。
- **既定のテンプレートが敷いている装飾は、色を指定しても消えない。** `ContextMenu` は左端にアイコン用の白い帯を持っており、`Background` を変えても残る。項目を自前で描くならテンプレートごと差し替えること。**メニュー内の区切り線は `{x:Static MenuItem.SeparatorStyleKey}` という専用のキーで引かれる**ので、`Separator` の暗黙スタイルは当たらない。
- **クリックの出どころから上へたどるときに `VisualTreeHelper.GetParent` をそのまま使わない。**
  `TextBlock` の中を色分けすると（差分の色分けが `Run` を並べる）、マウスの `OriginalSource` が
  **`Run`＝`ContentElement` で Visual ではない**ため、渡した瞬間に `InvalidOperationException` が飛び、
  **アプリごと落ちる**（2026-08-17 に「差分の結果を押したら落ちた」として報告された）。
  たどり方は `MainWindow.ClickedParentOf` 1 か所に閉じてあり、`ContentElement` は
  論理の親（＝載っている `TextBlock`）へ上げてから続ける。**新しい経路でも必ずこれを通すこと。**
- **押しても何も起きないボタンを、押せるままにしない。** 「比較を停止」は結果を閉じて
  貼り付け欄に戻るだけのボタンで、貼り付け欄を出している間も押せたため
  「押しても効かない」と報告された（2026-08-17）。`CanExecute` で状態を映し、
  **名前も実際にすることに合わせる**（「貼り付けに戻る」に改めた）。
- **`ComboBox` に `IsEditable="True"` を使わない。** このアプリは `ComboBox` のテンプレートを置き換えており、**`PART_EditableTextBox` を持っていない**。付けると入力欄が描かれず、**選んでも表示は空のまま**になる（ACI タブの接続先で実際にやった。2026-08-17 にユーザーが実機で発見）。手で打つ欄は素の `TextBox` にする。自己診断が全タブの `ComboBox` を見て捕まえる。
- **`ComboBox` の閉じているときの表示に `DisplayMemberPath` は効かない。** 選択中の項目は `SelectionBoxItemTemplate` 経由で描かれるため、**型名がそのまま出る**。ドロップダウンを開いたときだけ正しく見えるので気づきにくい。`ItemTemplate` を明示すること。
- **測定結果ごとに `Dispatcher.Invoke` しない。** 500 宛先 × 1Hz = 500 通知/秒で UI が溶ける。`Channel<T>` に積んで 10Hz の単一ポンプでまとめて適用する。
- `ObservableCollection` の**構造変化は宛先の追加/削除時のみ**。測定結果は既存の行 VM のプロパティ更新として流す。
- 一覧は仮想化必須（`VirtualizationMode=Recycling`、行高固定）。ソート・フィルタはティックごとに `Refresh()` しない。**仮想化の指定は ListBox 側の添付プロパティに書く。** `VirtualizingStackPanel` はパネル要素に書いた属性ではなく ItemsControl 側を読むため、`<VirtualizingStackPanel VirtualizationMode="Recycling" />` は無言で無視される（実際にやらかした）。折り返し（タイル敷き詰め）の一覧は WrapPanel では仮想化できない。かつて自作の仮想化パネルで実装したが、**「1 行に複数エントリを並べない」というユーザー指示で 2026-08-15 に一望表示ごと削除した**。再提案しないこと。
- **文字の大きさは 4 段階から選べる**（`App/UiScale.cs`。小 0.85 / 標準 1.0 / 大 1.25 / 特大 1.5）。
  仕組みは配色と似ているが**辞書は差し替えない** — `Application.Resources` に計算した値を直接入れる。
  `Tokens.xaml` が「標準」の定義で、**あちらの値は書き換えない**（起動時に 1 度読んで基準にする）。
  - **寸法も必ず `DynamicResource` で引く。** 直書きすると文字サイズに追随しない。
    行の高さは `Size.RowHeight`（素の行）/ `Size.RowHeight.Wide`（コンボの入る行）/
    `Size.RowHeight.Input`（`PasswordBox` の入る行）から引くこと。
  - **ツールチップの幅は数で持たない。** `Size.TooltipWidth` を `Size.Body × 68 + 24` で毎回出す。
    こうすると「1 行は全角 68 字」という決まりが倍率に依らず成り立ち、自己診断も直さなくて済む。
  - **列幅は `UiScale.Changed` を拾って比例させる**（`MainWindow` が購読）。文字だけ大きくすると、
    幅がピクセルで決まっている列で文字が切れる。`Reset()` の既定値にも倍率を掛けること。
  - **自己診断で倍率を変えたら必ず戻す。** 既定幅 880px とタブ実測幅の突き合わせ、
    見出しの余白、ⓘ の 68 字の検査がすべて標準の大きさを前提にしている。
- **`MenuItem` のテンプレートはチェック印を自前で描く。** 既定テンプレートを置き換えているので、
  印を描く要素を消すと `IsCheckable="True"` にしても状態が画面に出ない
  （「最前面に固定」が入っているか分からない、という不具合になっていた）。
- **配色は明暗 2 つある。色は必ず `DynamicResource` で引く。** `ThemeManager` が `Palette.xaml` / `Palette.Dark.xaml` を丸ごと差し替えるため、`StaticResource` で引いた色は切り替えても古いまま残る。**コードから `TryFindResource` した結果を static にキャッシュしない**（Sparkline で実際にやって切替に追随しない不具合になった。依存プロパティで受け取る形に直してある）。色を足すときは**両方のパレットに同じキーを**足すこと（自己診断が突き合わせている）。色に依らない寸法・書体は `Tokens.xaml`。
- **文字と地のコントラストは自己診断が検算する。** 淡くしたい気持ちと読めることは必ず衝突するので、目視で決めない。`Core/Design/ColorMath.cs` の WCAG 比で 4.5 以上を満たすこと。
- **`DropShadowEffect` を一覧まわりで使わない。** 中身が毎秒書き換わるので、効果を挟むと中間サーフェス経由の再描画になる。立体感は「上辺だけ明るいグラデーションの枠」で出す（`Brush.Card.Edge`）。
- **画面に出す文言では「口」と書かない。**「ポート」（機器のポート）か「インターフェース」を使う
  （2026-08-17 ユーザー指示）。「設定を書く口」のような言い回しも、画面と利用手引きでは
  「設定を書き込む機能」に言い換える。コードのコメントは対象外。
- **空の一覧には `EmptyHint` を重ねる。**一覧を `Grid` で包み、
  `Visibility="{Binding Items.Count, ElementName=<一覧の x:Name>, Converter={StaticResource ZeroToVisible}}"`
  の TextBlock（`Style="{StaticResource EmptyHint}"`）で「何を押すとここに出るか」を書く。
  **ElementName の打ち間違いはバインドが黙って死ぬ**ので、xUnit（埋め込み XAML の字面）が
  全数の飛び先と文言を突き合わせる。実描画で数える検査は CI で 0 個としか数えられず取りやめた
  （原因未特定。視覚ツリーの Tag が拾えない）。
- **進行の帯は `BusyBar` スタイル（高さ 3px・操作列の直下）で統一。ProgressBar を直に置かない**
  （xUnit が全 ProgressBar の Style を検査）。割合を持たない待ちは `IsIndeterminate="True"` —
  共通テンプレートが満タンの帯を明滅させる（WPF は不定モードで帯を全幅にするだけなので、
  そのままだと 100% 完了と見分けが付かない）。
- **取り消せない消去と同名上書きは ConfirmDialog を挟む**（2026-08-20 の UI 改善）。
  対象: 履歴を消去（Ping/TCP）・受信一覧を消す（5 画面）・IP プリセット削除・
  別名で保存したら既存名と衝突したとき。**行単位の ✕ 削除には挟まない**（1 行ずつで目に見える）。
  確認はボタン経路だけに掛け、**内部の掃除（Reset /「すべて消す」）は確認なしの実体を呼ぶ** —
  分けないと「すべて消す」の 1 回の確認のあとに 5 画面ぶんのダイアログが続く。
- **初期 Status を空にしない。**各 VM は `IdleHint`（何をすれば動くかの一言）を持ち、
  初期値と `Reset()` の戻し先にする（2026-08-20 の UI 改善。自己診断が起動直後の非空を検査）。
  例外は `ReportViewModel` — メニュー帯に常駐するので、常時ヒントを出すと騒がしい。
- **行コンテナを新設したら `IsKeyboardFocused` のトリガも付ける**（下線の BorderBrush を
  Brush.Accent.Fg に。応答なし欄だけは Error.Fg — あの欄は緑・アクセントを使わない決まり。
  太さは変えない: 変えると行の高さが揺れて仮想化中にガタつく。2026-08-20 の UX 改善）。
- **`ResultText` は `SetResult(text, kind)` 経由でのみ書く**（IpConfig / Proxy。
  文言と色種別を同時に決め、片方だけ書ける口を残さない。成功=Ok（緑）・失敗=Alert（赤）・
  進行/リセット=Muted。2026-08-20 の UX 改善）。
- **1 行の実行欄には Enter を結線する**（`InputBindings` の `KeyBinding Key="Enter"`。
  2026-08-20 の UX 改善。IME 処理済みのキーは `Key.ImeProcessed` で届くので、変換確定の
  Enter では発火しない）。**結線しない欄**: 複数行（Enter は改行）／ IP設定・プロキシの適用
  （UAC を伴う変更操作に Enter の誤爆を作らない）／ ログ採取の開始（機器へ接続しに行く）。
- **記号の規約**: ✓=成功 / △=注意 / ▶=進行中 / ✕=失敗・不達 / ◌=待機 / ⚠=Notice の頭。
  **✔ と ▲ は使わない**（▲▼ はソート印専用）。● は Ping の「応答あり」専用。
- **状態は色だけで表さない。** 緑と赤は色覚多様性下で潰れる。`● 応答` / `✕ 不達` / `⊘ 拒否` のように記号と文字を必ず併記し、色は補助にとどめる。（「▲ 遅延」の状態表示は 2026-08-15 にユーザー指示で廃止。再提案しない）
- **地色と文字色はパレット表のペアで使う。** 地色を敷くのは状態バッジだけ。数値は文字色のみで表現する。
  **例外は無線タブの周辺 AP 一覧**（2026-08-18 ユーザー指示で行ごと塗り分けた）。
  閾値は `WifiSignalGuide.KindOf`（`Describe` と同じ切れ目）で Core に置き、
  行には dBm を出したままにする（色だけに頼らせない）。薄く（`Opacity` 0.6）敷くのは、
  選んでいる行が下から透けるようにするため。
- グラフで **RTT と損失率を 2 軸で重ねない**。存在しない相関を作り出す。分けて描く。

## アイコン

元は `src/NetworkToys.App/Resources/AppIcon.svg`（**おもちゃ箱**。箱の口から積み木が覗く形。
地は敷かず透過、箱は水色の 2 色・積み木はミント/ピーチ/ローズ。
2026-08-17 に RJ45 の端子から描き替えた）。`tools/icon/build_icon.sh` を**手で実行**して
16〜256px の 9 サイズを焼き、`.ico` にまとめる
（CI に入れると ImageMagick を毎回入れることになるので入れていない）。

- **グラデーションを使わない。** スクリプトが使う ImageMagick の内蔵 SVG レンダラは
  `linearGradient` を解釈できず、**黒く塗り潰したまま何の警告も出さずに焼き上がる**。
  べた塗りだけで描くこと（`rsvg-convert` を入れれば通るが、そのために必要な道具は増やさない）。
- **24px 以下は `AppIcon.Small.svg` から焼く。** 通常版を縮めると積み木 3 つが 1px 台になって
  箱の上が濁る。小さいところは積み木を 2 つに減らして大きくし、箱の縁を厚くした別の絵にしてある
  （丸は小さくすると角丸の四角と見分けが付かないので、そちらでは四角と三角だけ）。
  1 枚で全サイズを賄わない。色と配置は 2 枚で揃えてあるので、並んでも同じ絵に見える。
- **絵の中身は誰も検査しない。** 自己診断はアイコンの個数しか見ないので、
  焼いたら `.ico` から 16/32px を取り出して**目で見る**こと
  （16px で箱と積み木と分かるかが合否）。この開発環境には `convert` が入っているので、
  焼くところまではこちらでできる。
- **描き替えるときは案を並べて焼いてから決める。** 16px でどう見えるかは描いた絵からは分からない
  （実際、ふたを開けた案は「浮いた板」に見え、プラグの案は「箱の上に載った機械」に見えた）。
  候補を `tools/icon/candidates/` に置いて焼き、決まったら候補ごと消す。
- 埋め込みは csproj の `ApplicationIcon`。**指定漏れやパスの誤りはビルドを通る**ので、
  自己診断が `shell32` の `ExtractIconEx` で exe から数えて確かめている
  （`System.Drawing` は `UseWPF` だけでは参照されないため使えない）。

## 使い方の同梱

`docs/USAGE.md` を**配布物に「使い方.md」として置く**（2026-08-18 ユーザー指示）。
同梱は csproj の `Content` 1 か所で、**CI は入っていることを確かめるだけ**（手元の publish でも同じものが入る）。

- **持ち出して使う道具**なので、ネットワークの無い現場でも読める形にしておく。
- **中身は Markdown なので拡張子も `.md`**。
- **アプリの中では開かない**（「ヘルプ → 使い方」は 2026-08-18 に足してすぐ削除した。再提案しないこと）。
- 自己診断は「exe の隣にファイルがあること」を見ている。

## 配る資料（docx）

`docs/紹介資料.md` / `docs/技術資料.md` / `docs/設計資料.md` を `tools/docs/build_docx.py` で `.docx` に焼く（**手で実行**）。
焼いたものもリポジトリに置いてある（受け取る人が Word で開くだけで済むように）。

- **ライブラリを足さない。** docx は zip に XML を 4 枚入れただけのもので、標準の `zipfile` で足りる
  （xlsx と同じ判断。そもそもこの開発環境には pip が無い）。**python-docx を再提案しないこと。**
- **Word は要素の順番に厳しい。** `w:pPr` は `pStyle → keepNext → spacing → ind → outlineLvl → rPr`、
  `w:rPr` は `rStyle → rFonts → b → i → color → sz` の順。**逆に書くと「読めない内容」でファイルごと開かない**
  （xlsx でシートの要素順を間違えたときと同じ壊れ方）。書式が重なる場面（太字の中の等幅、注記の中のコード）で
  rPr を繋ぎ合わせると **1 つの run に rPr が 2 つ**入って同じ結果になる。組み立ては `run_properties` 1 か所に閉じてある。
- **実物を開いて確かめられない**ので、スクリプト自身が焼く前に検査する（整形式・要素の並び・rPr の重複・
  表の直後の段落・本文末尾の `sectPr`）。ここを緩めない。
- 内容を直したら **`.md` を直して焼き直す**。`.docx` を直接いじらない。
- **図の原本は SVG**（`docs/figures/*.svg`）。`sh tools/docs/build_figures.sh` で PNG に焼き、
  資料には `![説明](figures/x.png)` と書く。**PNG もリポジトリに置く** — docx を焼くときに
  rsvg-convert を要求しないため。**docx に SVG は入れない**（読めない Word の版がある）。
  図は本文いっぱい（A4 の余白を引いた 9070 twips）に合わせ、高さは元の縦横比から出す。
  **`wp:docPr` の id と `rId` は 1 から通し**で、`document.xml.rels` の並びと必ず揃える。
  揃え（`w:jc`）は `w:ind` の後・`w:outlineLvl` の前（要素順の検査に入れてある）。
- **図は焼いて目で見る。**この開発環境には `rsvg-convert` があるので PNG にして確認できる
  （日本語は IPAGothic で描かれる）。**箱から文字がはみ出していないか**は、
  実際に見るまで分からない（実際に 2 度直した）。

## 設定の保存先

**exe と同じフォルダ**。`AppData.PathOf()` を通すこと。持ち出して使うための作り。
書き込めない場所（Program Files など）に置かれたときだけ `%APPDATA%\NetworkToys\` へ逃がす。
場所は**実際に 1 ファイル書いて消して**決めている。属性だけでは判定できないため。
旧アプリ名（PingWatcher / PastelNet）のフォルダは見ない（2026-08-17 ユーザー指示）。逃がし先に中身があるときだけ初回に一度複製して引き継ぐ。

**設定は `settings.json` の 1 ファイルに統合**（2026-08-16、ユーザー指示）。配色・Ping/TCP の宛先リストと測定既定値がすべて入る（**列幅は入らない** — 保存しない決まり）。入り口は `App/Settings.cs`（起動時に `Settings.Initialize()` — **`ThemeManager.Initialize()` より前**でないと配色が読めない）。旧 4 ファイル（targets.json / tcp-targets.json / theme.txt / columns.txt）は初回起動時に取り込み、統合ファイルを書けたときだけ削除する。新しい設定を足すときは `AppSettingsDocument` にプロパティを足し、`Settings.Current` を書き換えて `Settings.Save()` を呼ぶ（ファイルを増やさない）。

## 構成

```
src/NetworkToys.Core/   純粋ロジック。Windows 非依存。ここを厚くする
src/NetworkToys.App/    WPF 本体。Core を参照する（逆参照は禁止）
tests/                Core のユニットテスト
tools/                OUI テーブル生成などの手動スクリプト
```

## フェーズ

Phase 0 骨組みと CI ／ 1 並列 ping ／ 2 TCP ping・DNS・traceroute ／ 3 IP スキャン ／ 4 無線 LAN ／ 5 回線品質 ／ 6 レポート。
Phase 1 完了時点で「EXPing の不満を解消する」という当初目的は達成される。

## 確認の依頼先

CI で分かるのはビルド成否・ユニットテスト・起動可否まで。**見た目と実機の挙動（配色の印象、数百宛先での描画、Wi-Fi API の実際の応答）はユーザーに確認してもらう**しかない。フェーズ完了時にスクリーンショットを依頼すること。
